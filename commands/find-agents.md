调用 `find-agents` 技能发现可用代理。

参数：`$ARGUMENTS`

典型触发语（用户常见说法）：
- "有哪些 agent 可以用 / 推荐几个代理"
- "这个任务该调用哪些专家 / 并行调用哪些代理"
- "帮我找一下审查文档 / 写代码 / 分析影响该用谁"

执行要求（加强）：
- 只返回 `.claude/agents/` 根目录下真实存在的 `.md` 文件名（不含 `.md`），禁止编造不存在的代理。
- 返回的代理列表默认设计为可同时调用，不得隐含顺序依赖。
- 须为每个代理生成"必答问题"，明确输入和输出期望。
- 代理返回仅作候选，最终是否写入文档由调用方决定。

请使用 Skill 工具调用 `find-agents` 技能，传入上述参数，最后返回推荐代理列表与必答问题。
