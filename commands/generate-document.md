调用 generate-document 技能生成文档。

参数：$ARGUMENTS

常用示例：

- `/generate-document <功能名>-<一句话描述>`：生成 `docs/<功能名>/` 下全文档编号集（01-05, 07）
- `/generate-document weekly`：生成周报（单文档），落盘到 `docs/周报/<YYYY-Www>_周报.md`

请使用 Skill 工具调用 generate-document 技能，传入上述参数。