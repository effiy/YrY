调用 `implement-code` 技能实施代码。

参数：`$ARGUMENTS`

典型触发语（用户常见说法）：
- "实现这个功能 / 把这个需求写成代码"
- "按设计文档开发 / 开始编码"
- "完成需求任务里的场景"

执行要求（加强）：
- 必须基于已存在的 `docs/<功能名>/02_需求任务.md` 和 `03_设计文档.md` 实施，不得脱离上游文档凭空编码。
- 实施完成后必须写入 `06_实施总结.md`，并执行 `import-docs` → `wework-bot` 收尾。
- 无法找到上游文档时，须明确说明缺失文件路径并中断。

请使用 Skill 工具调用 `implement-code` 技能，传入上述参数，最后返回实施结果与验证结论。
