/**
 * qsCoverageGaps · config data source
 * ----------------------------------------------------------------------
 * Coverage-gaps callout panel. Used below the hero path.
 *
 * Exposed via window.QS_COVERAGE_GAPS_CONFIG and read by index.js.
 */
window.QS_COVERAGE_GAPS_CONFIG = {
    templateId:    'qs-coverage-gaps-tpl',
    loadTimeoutMs: 5000,
    defaults: {
        hint: ''
    }
};
