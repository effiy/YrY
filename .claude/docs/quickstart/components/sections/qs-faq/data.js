/**
 * qsFaq · config data source
 * ----------------------------------------------------------------------
 * FAQ section body. Q&A pairs with collapsible state per question.

 * Exposed via window.QS_FAQ_CONFIG and read by index.js.
 */
window.QS_FAQ_CONFIG = {
    templateId:    'qs-faq-tpl',
    loadTimeoutMs: 5000,
    defaults: {}
};
