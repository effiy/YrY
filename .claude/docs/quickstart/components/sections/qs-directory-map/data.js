/**
 * qsDirectoryMap · config data source
 * ----------------------------------------------------------------------
 * Directory-map section body. Preformatted tree + annotations grid.

 * Exposed via window.QS_DIRECTORY_MAP_CONFIG and read by index.js.
 */
window.QS_DIRECTORY_MAP_CONFIG = {
    templateId:    'qs-directory-map-tpl',
    loadTimeoutMs: 5000,
    defaults: {}
};
