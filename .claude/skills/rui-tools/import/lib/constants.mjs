/**
 * rui-import constants -- shared constants inlined from _shared/constants.mjs
 * Originally lived in .claude/skills/_shared/; inlined here so the skill is
 * self-contained and runnable without external shared modules.
 */

export const NODE_ARGV_OFFSET = 2;
export const DEFAULT_API_URL = "https://api.effiy.cn";
export const CONCURRENCY = 4;
