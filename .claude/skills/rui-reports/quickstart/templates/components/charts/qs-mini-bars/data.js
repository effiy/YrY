/**
 * qsMiniBars · config data source
 * ----------------------------------------------------------------------
 * Compact group bar list. Used by the score banner skill-group breakdown.
 *
 * Exposed via window.QS_MINI_BARS_CONFIG and read by index.js.
 */
window.QS_MINI_BARS_CONFIG = {
    templateId:    'qs-mini-bars-tpl',
    loadTimeoutMs: 5000,
    defaults: {}
};
