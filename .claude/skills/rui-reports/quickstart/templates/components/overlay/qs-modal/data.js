/**
 * qsModal · config data source
 * ----------------------------------------------------------------------
 * Command detail modal. Opens when a command/concept is selected.

 * Exposed via window.QS_MODAL_CONFIG and read by index.js.
 */
window.QS_MODAL_CONFIG = {
    templateId:    'qs-modal-tpl',
    loadTimeoutMs: 5000,
    defaults: {}
};
