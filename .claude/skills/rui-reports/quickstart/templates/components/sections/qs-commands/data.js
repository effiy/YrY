/**
 * qsCommands · config data source
 * ----------------------------------------------------------------------
 * Commands section body. Click-to-open modal for each command.

 * Exposed via window.QS_COMMANDS_CONFIG and read by index.js.
 */
window.QS_COMMANDS_CONFIG = {
    templateId:    'qs-commands-tpl',
    loadTimeoutMs: 5000,
    defaults: {}
};
