/**
 * qsHeroPath · config data source
 * ----------------------------------------------------------------------
 * 5-step onboarding node graph. Used by the hero panel.
 *
 * Exposed via window.QS_HERO_PATH_CONFIG and read by index.js.
 */
window.QS_HERO_PATH_CONFIG = {
    templateId:    'qs-hero-path-tpl',
    loadTimeoutMs: 5000,
    defaults: {}
};
