---
paths:
  - ".claude/**"
---

# rui-claude

> 操作范围限定在 `.claude/` 目录。

## 适用

YrY 的 `/rui-claude` 命令。

## 规则

1. 所有变更限定在 `.claude/` 范围
2. sync 拉取远端最新
3. retro 健康复盘
4. history 操作回溯
5. 不修改 `.claude/` 范围外的文件
