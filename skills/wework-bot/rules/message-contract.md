# wework-bot 消息契约

## API 契约

默认请求：

```text
POST https://api.effiy.cn/wework/send-message
```

请求体：

```json
{
  "webhook_url": "<企业微信机器人 webhook>",
  "content": "<消息正文>"
}
```

认证：

- Header 使用 `X-Token`
- `X-Token` 来自 `--token` 或 `API_X_TOKEN`

## webhook 配置

支持五种方式，优先级从高到低：

1. `--webhook-url`：完整 webhook URL
2. `--webhook-key`：只传 key，脚本自动拼接标准 webhook
3. `WEWORK_WEBHOOK_URL` / `WEWORK_WEBHOOK_KEY`：全局环境兜底机器人
4. `--robot` + `--config`：从配置文件的 `robots` 中选择机器人
5. `--agent` + `--config`：从配置文件的 `agents` 映射到机器人
6. 配置文件中的 `default_robot`：未传 `--config` 时，脚本会在存在本地文件时自动加载 `.claude/skills/wework-bot/config.local.json`

```text
https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=<key>
```

## 多机器人配置

配置文件结构：

```json
{
  "default_robot": "general",
  "robots": {
    "general": {
      "webhook_key_env": "WEWORK_WEBHOOK_KEY_GENERAL"
    },
    "security": {
      "webhook_key_env": "WEWORK_WEBHOOK_KEY_SECURITY"
    }
  },
  "agents": {
    "generate-document": "docs",
    "import-docs": "docs",
    "wework-bot": "general",
    "implement-code": "code",
    "security-reviewer": "security",
    "code-reviewer": "general"
  }
}
```

机器人支持字段：

| 字段 | 说明 |
|------|------|
| `webhook_url` | 完整 webhook，不推荐提交真实值 |
| `webhook_url_env` | 从环境变量读取完整 webhook |
| `webhook_key` | webhook key，不推荐提交真实值 |
| `webhook_key_env` | 从环境变量读取 webhook key |
| `api_url` | 可选，覆盖默认发送 API |

推荐只提交 `config.example.json`，真实配置放在未提交的 `config.local.json` 或环境变量中。默认机器人需要完整 URL 时使用 `webhook_url` 字段；需要按环境隔离时使用 `webhook_url_env` 或 `webhook_key_env`。

## 消息格式

### 阶段通知（通用格式）

```text
📣 <流程名> <状态>
━━━━━━━━━━━━━━━━━
🎯 结论：<一句话说明结果和影响>
🛠️ 流程：<流程名>
📌 功能：<功能名或文档路径>
📝 描述：<100字以内描述>
📍 阶段：<阶段名>
📊 状态：<开始 / 通过 / 阻断 / 失败 / 需要人工介入>
🔎 关键：<1-2 个关键事实或原因>
🤝 AI 调用：skills <N> / agents <N> / mcp <N> / tools <N>
🔗 调用链：<find-skills→...→wework-bot>
📁 测试路径：<tests/ 内 spec/page/fixture 数量与逸出情况>
🧫 测试统计：<spec/case/通过/失败>（如有）
🔁 修复轮次：<阶段2: N 轮 / 阶段6: N 轮>（如有）
📦 产物：<代码 / 测试 / 文档>（如有）
📈 指标：<lint / 影响链 / 冒烟时间>（如有）
🌐 影响：<影响范围、交付范围或风险范围>
📎 证据：<报告路径 / 命令 / MCP 调用序列 / 结果摘要>
📂 报告：<docs/<功能名>/06_实施总结.md 或对应文档路径>
☁️ 文档同步：docs → YiAi（创建 N，覆盖 N，失败 N）
⏱️ 用时：<本次会话总耗时>
🪙 会话用量：<本次会话 Token 或用量摘要，与 Cursor 可核对>
🟢 开始时间：<YYYY-MM-DD HH:mm:ss>（如有）
🌿 分支：<git-branch>
🔖 提交：<short-sha>
🤖 模型：<模型名>
🧰 工具：<主要工具清单>
🕒 最后更新：<YYYY-MM-DD HH:mm:ss>
👉 下一步：<需要接收者执行的动作；无需行动时写"无需人工介入">
```

状态值建议：`开始` / `通过` / `阻断` / `失败` / `需要人工介入`

电梯法则要求：每条推送必须让接收者在 10 秒内读懂"发生了什么、影响是什么、证据在哪里、下一步做什么"。正文必须分行显示，禁止把结论、描述、关键事实和下一步塞进同一行。

描述要求：每条推送必须包含 `描述：` 或 `📝 描述：` 行，内容不超过 100 字，用于说明本次推送的背景、结果或下一步动作。

业务度量要求（适用于完成、阻断、门禁异常通知）：

- 必须根据本次执行的真实情况补齐 `🤝 AI 调用` / `🔗 调用链`，二者数据应与 `06_实施总结.md` 中的「AI 调用记录」「§1 AI 调用流程图 / §2 AI 调用时序图」一致
- 若已生成 `06_实施总结.md`，应补齐 `📐 实施总结图表`（§1 节点数、§2 参与者数或等价摘要）、`🧩 MCP 明细`（各 browser_* 次数或汇总）、`🧾 待办与风险`（P1/P2 与 §9 对应）、`🗂️ 状态回写`（`01/02/.../07` 与 §5.5 结论）；可通过 `send-message.js` 的 `--diagram-summary`、`--mcp-breakdown`、`--backlog`、`--status-rewrite` 传入
- 必须基于 `artifact-contracts.md §2.2 测试路径门禁扫描命令` 真实结果填写 `📁 测试路径`，未执行扫描时不得发送
- 凡是涉及到测试文件的产物，必须在数字中区分 `tests/` 内与 `tests/` 外（`tests/` 外应为 0）
- `📦 产物` / `🧫 测试统计` / `🔁 修复轮次` / `📈 指标` 数字必须来自实际命令、MCP 操作或 git diff
- `📂 报告` 行必须指向实际生成的报告路径（固定为 `docs/<功能名>/06_实施总结.md` 或兜底项目日志目录）

元信息要求：每条推送必须包含 `🤖 模型：...`、`🧰 工具：...`、`🕒 最后更新：YYYY-MM-DD HH:mm:ss` 三行。时间必须精确到秒；若调用方未传入，发送脚本必须使用本地当前时间自动补齐。`🌿 分支` / `🔖 提交` 由脚本自动从 git 检测，可通过 `--no-auto-git` 关闭。

**会话成本（必含）**：`⏱️ 用时`（本次会话耗时）与 `🪙 会话用量`（本次会话 Token/用量）必须在长流程完成、阻断、门禁异常及建议含度量的阶段通知中出现；若正文未写且未传 `--duration` / `--token-usage`，`send-message.js` 会注入与 Cursor 核对的缺省说明行，**禁止**留空。

会话与复盘（长流程完成/阻断类推送须**可核对**）：

- `⏱️ 用时` / `🟢 开始时间`：与本次会话/执行真实起止一致；未记时可使用脚本缺省句或写「未记录」，**禁止**猜测秒数冒充精确值。
- `🪙 会话用量`：须与 Cursor 用量面板、账单或导出一致；无法取得时使用脚本缺省句或「未提供，请从 Cursor 用量核对」，**禁止**编造。
- `💡 改进建议`：只写与**本次已发生**问题相关的可执行项（如证据链缺口、可自动化步骤），≤500 字；避免空泛「加强测试」类套话。

正文转义：`content` 中不得向用户展示字面量 `\n`（反斜杠加字母 n）。多行正文应使用真实换行（`--content-file` 推荐），或由 `send-message.js` 在加载后对误写 `\\n` / `\\t` 做归一化。

### 生动总结模板·动态生成（强制）

> `generate-document` 和 `implement-code` 每次执行结束，**必须**调用 `message-pusher` agent 动态生成推送文案，不得直接套用静态模板。
> `message-pusher` 基于 Plan 模式整理本次执行的事实与数据，逐项填充所有必含字段后交付文案。
> 以下为**参考结构**（非静态模板），实际内容由 `message-pusher` 基于真实执行数据组装。

**必含字段（所有完成/阻断通知，`message-pusher` 必须逐项填充）：**

| 字段 | emoji | 数据来源 |
|------|-------|---------|
| AI 调用 | `🤝` | 本次会话实际调用记录（skills/agents/mcp/tools 四维度） |
| 调用链 | `🔗` | 本次会话实际调用顺序（紧凑链路） |
| 开始时间 | `🟢` | `.claude/session-time.json` / 会话记录 |
| 结束时间 | `🔴` | `.claude/session-time.json` / 会话记录 |
| 用时 | `⏱️` | 结束时间 - 开始时间 |
| 会话用量 | `🪙` | Cursor 用量面板 / 环境变量 |
| 改进建议 | `💡` | 本次已发生事实（2-4 条可执行项，禁止泛泛而谈） |
| 下一步 | `👉` | 流程状态推断（具体动作，"无需人工介入"仅在确认无需时使用） |
| 模型 | `🤖` | 环境变量 |
| 工具 | `🧰` | 环境变量 / 本次实际调用 |
| 最后更新 | `🕒` | 本地当前时间（YYYY-MM-DD HH:mm:ss） |

**generate-document 完成通知参考结构：**

```text
📄 文档生成完成
━━━━━━━━━━━━━━━━━
🎯 结论：<一句话结果和影响>
✅ 流程：generate-document
📌 功能：<功能名>
📝 描述：<100字以内描述>
📋 类型：<文档类型>
🔍 P0 自检：<通过 N / 共 N 项 或 失败 N 项>
🌐 影响：<文档覆盖范围 / 可进入的下一流程>
📎 证据：<生成文件路径 / 自检摘要>
🤝 AI 调用：skills N / agents N / mcp N / tools N
🔗 调用链：<spec-retriever→...→wework-bot>
📦 产物：<新生成 N 份 / 更新 M 份>
☁️ 文档同步：docs → YiAi（创建 N，覆盖 N，失败 N）
⏱️ 用时：<Hh Mm Ss>
🪙 会话用量：<Token 或用量摘要>
🟢 开始时间：YYYY-MM-DD HH:mm:ss
🔴 结束时间：YYYY-MM-DD HH:mm:ss
💡 改进建议：1. <基于本次事实的改进项>；2. <...>
🌿 分支：<git-branch>
🔖 提交：<short-sha>
🤖 模型：<模型名>
🧰 工具：<主要工具清单>
🕒 最后更新：YYYY-MM-DD HH:mm:ss
👉 下一步：<基于流程状态的具体下一步动作>
```

**implement-code 完成通知参考结构：**

```text
🚀 代码实施完成
━━━━━━━━━━━━━━━━━
🎯 结论：<一句话结果和影响>
✅ 流程：implement-code
📌 功能：<功能名>
📝 描述：<100字以内描述>
📊 阶段：<0 → 8（全部通过）>
🧪 P0 检查项：<全部通过（共 N 项）>
🌐 影响：<已交付价值 / 受影响模块 / 发布影响>
📎 证据：<测试命令 / 报告路径 / MCP 操作摘要>
🤝 AI 调用：skills N / agents N / mcp N / tools N
🔗 调用链：<find-skills→...→wework-bot>
📁 测试路径：<tests/ 内 N spec / N page，无逸出>
🧫 测试统计：<spec N / case N / 通过 N / 失败 N>
🔁 修复轮次：<阶段2: N 轮 / 阶段6: N 轮>
📦 产物：<代码 N / 测试 N / 文档 N>
📐 实施总结图表：<§1 N 节点 / §2 K 参与者>
🧩 MCP 明细：<nav N / snap N / click N / eval N>
🧾 待办与风险：<P1 N / P2 N（无则写「无」）>
🗂️ 状态回写：<01-07 已更新>
📂 报告：<docs/<功能名>/06_实施总结.md>
☁️ 文档同步：docs → YiAi（创建 N，覆盖 N，失败 N）
⏱️ 用时：<Hh Mm Ss>
🪙 会话用量：<Token 或用量摘要>
🟢 开始时间：YYYY-MM-DD HH:mm:ss
🔴 结束时间：YYYY-MM-DD HH:mm:ss
💡 改进建议：1. <...>；2. <...>；3. <...>
🌿 分支：<git-branch>
🔖 提交：<short-sha>
🤖 模型：<模型名>
🧰 工具：<主要工具清单>
🕒 最后更新：YYYY-MM-DD HH:mm:ss
👉 下一步：<具体下一步动作>
```

**格式强制要求：**

- 每条信息独占一行，禁止将多条信息写在同一行
- 使用 `━━━` 分隔线分隔标题行与正文
- 必须包含 `🎯 结论：...` 行，放在分隔线后第一行；结论必须是一句话
- 必须包含 `📝 描述：...` 行，描述内容必须为 100 字以内
- 完成、阻断、门禁异常通知必须包含 `🌐 影响：...` 与 `📎 证据：...`
- 完成通知必须包含 `📦 产物：...` 或等价产物统计；阻断通知必须包含 `🧭 恢复点：...`
- 必须包含 `🤖 模型：...`、`🧰 工具：...`、`🕒 最后更新：YYYY-MM-DD HH:mm:ss` 三行；须同条包含 `⏱️ 用时` 与 `🪙 会话用量`（脚本可补缺省句）
- 必须包含 `👉 下一步：...` 行；无需行动时写“无需人工介入”
- 数字必须来自实际执行结果，**禁止**直接发送含 `<占位符>` 的模板
- 消息总长度建议不超过 800 字；门禁失败、门禁失效、流程阻断类消息不得删减原因、证据、模型、工具和时间
- `☁️ 文档同步` 行若 `docs` 不存在，改为：`☁️ 文档同步：docs 不存在，跳过导入`

### 门禁失败 / 门禁失效模板（强制格式）

> 任意门禁失败、门禁失效、门禁证据缺失或阻断收尾失败时必须发送，不得只在聊天中说明。

```text
⛔ 门禁异常
━━━━━━━━━━━━━━━━━
🎯 结论：<门禁未通过 / 未执行 / 证据缺失，当前流程已停止>
🛠️ 流程：<implement-code / verification-loop / 其他>
📌 功能：<功能名或任务路径>
📝 描述：<100字以内描述>
🔍 门禁：阶段 <N>（<门禁名称>）
📎 证据：<报告路径 / 命令 / MCP 工具序列 / 失败摘要>
📊 结果：通过 <N> / 失败 <M> / 未执行 <K>
❌ 原因：<1-2 条明确原因>
🌐 影响：<阻断范围、不可发布范围或需要人工确认的范围>
🧭 恢复点：<从哪个阶段恢复、先做哪一步>
⏱️ 用时：<本次会话总耗时>
🪙 会话用量：<Token 或用量摘要>
🤖 模型：<模型名>
🧰 工具：<主要工具清单>
🕒 最后更新：<YYYY-MM-DD HH:mm:ss>
👉 下一步：<恢复动作或人工介入要求>
```

## generate-document 观测点

- 开始生成时发送阶段通知（可使用 `--agent generate-document`）
- 开始生成全文档或关键单文档
- P0 自检失败并写入目标文档
- 缺参或缺少上游材料，已写入项目日志记录
- `import-docs` 同步完成后发送完成通知，`☁️ 文档同步` 行必须使用真实创建 / 覆盖 / 失败数字

## import-docs 观测点

- `docs` 标准导入开始
- 导入完成并产生 created / overwritten / failed 汇总
- 导入失败但主流程继续交付时，向 `wework-bot` 提供失败摘要

## implement-code 观测点

- 阶段 0 文档预检完成
- 阶段 2 动态检查门禁失败或通过
- 阶段 4 模块实施完成
- 阶段 6 冒烟测试失败或通过
- 阶段 7 实施总结已生成
- 任意阻断需要人工介入
- 任意应执行门禁未执行、无证据、被跳过或降级未记录

## 安全约束

- 不得提交真实 `X-Token`、完整 webhook URL 或 webhook key。
- 不得提交包含真实机器人密钥的 `config.local.json`。
- 日志和最终回复必须脱敏。
- 缺少 token 或 webhook 时必须停止，不尝试匿名发送。
- 使用 `--dry-run` 验证参数时不得实际发送消息。
- 若门禁异常通知发送失败，必须在实施总结或项目日志记录兜底中写明发送失败原因、脱敏路由、模型、工具和最后更新时间。
