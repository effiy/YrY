# 文档公式

> **定位**：所有 rui 文档的结构单一真相源。每份文档由一组「结构公式」（章节、表头、字段）规约，模型据此直接产出，不再依赖占位符模板。
>
> **公理推导**（[CLAUDE.md](../../CLAUDE.md)）：信模型 → 模型有能力按公式生成结构；惜注意 → 公式 < 模板（一文件 vs 29 文件）；退化对策「先可见，后规则」→ 公式即合约，结构本身在说话。
>
> **配套**：目录与生命周期见 [docs.md](./docs.md)；存储字段见 [data.md](./data.md)；生成时强制约束见 [rules/doc-generation.md](../../rules/doc-generation.md)。

## 通用元素（所有文档共用）

### F.meta — 版本头

```
> | v{version} | {YYYY-MM-DD} | {model} | {tool?} | 🌿 {branch} | ⏱️ {HH:mm}–{HH:mm} | 📎 [CLAUDE.md](../../../CLAUDE.md) |
```

- **必含**: version、date、model、branch
- **可选**: tool、time-range、philosophy 链接（仅 01-故事任务）
- **占位符必须替换**: 任何 `{...}` 留到产出即视为 P0 偏差

### F.nav — 导航块（01~04 文件首尾）

```
> **导航**: [← {prev}](./{prev-file}.md) · [↑ {index}](./00-索引.md) · [{next} →](./{next-file}.md)
```

- 00-索引: 不含导航块（自身是入口）
- 01: `[← 00-索引] · [↑ {类}文档] · [02-{section} →]`
- 04: `[← 03-{section}] · [↑ 00-索引]`（无 next）

### F.evidence — 证据等级

```
A=已验证(附路径) | B=可推导(附规则) | C=未验证(标注 `> 待补充`) | D=禁止(视为幻觉)
```

仅 A/B 直接写入；C 标注待补充；D 不得出现。

---

## 故事文档公式（docs/故事任务面板/{project}/{name}/）

### F.story.01 — 01-故事任务.md

**公式**: `meta + 角色公式速查 + Story×N + (§6 .claude改进? + §7 架构演进? + §L 自改进循环?)`

**角色公式速查**（顶部，固定文案）:
- PM: `作为 [角色] 我想要 [动作] 以便 [价值]`
- Tester: `Given [前置] When [操作] Then [预期]`
- Coder: `模块 → 接口 → 数据流`
- Security: `威胁 → 信任边界 → 缓解`

**每个 Story 章节**:

| 章节 | 负责人 | 表头/字段 |
|------|--------|----------|
| §1 Story | pm | 作为/我想要/以便/优先级(P0/P1/P2)/范围边界/依赖/子项目 + 范围外 |
| §1.1 User Operations | tester | `# \| 操作 \| 触发条件 \| 操作步骤 \| 预期结果`，UI 故事附 mermaid flowchart |
| §2 Requirements | pm | 功能点 `FP# \| 描述 \| 输入 \| 输出 \| 错误行为 \| 优先级`；业务规则 `R# \| 描述 \| 校验方式 \| 证据级别`；数据约束 `约束 \| 类型 \| 范围/格式 \| 来源` |
| §3 Design | coder + security | 设计概述（2-4 句）+ 决策表 `决策领域 \| 选定方案 \| 选择理由 \| 详见` |
| §4 Tasks | pm + 全员 | `ID \| 描述 \| 工作量(S/M/L) \| 依赖 \| 交付物 \| Agent \| 门禁 \| 交接下游` + 任务依赖 mermaid |
| §5 AC | tester | `AC# \| Given \| When \| Then \| 门禁(Gate A/B)` |
| §6 .claude 改进清单 | pm | 可选，skill/agent/rule/script/config 改进项 |
| §7 系统架构演进 | pm | 可选，近期/中期/远期 |
| §L 自改进循环 | self-improve | 可选，每次完成追加 |

**约束**: §1~§5 必填；§6/§7 按需；多故事时 `## Story 1`、`## Story 2` 顺序展开。

### F.story.02 — 02-后端技术评审.md

**公式**: `meta + nav + 服务架构 + API + 数据模型 + 安全 + 性能 + 评审清单`

| 章节 | 表头/内容 |
|------|----------|
| §1 服务架构 | 1.1 服务/进程 `变更类型 \| 模块/文件 \| 职责`；1.2 通信通道 mermaid sequenceDiagram + `通道 \| 方向 \| 协议 \| Payload \| 错误处理` |
| §2 API 接口 | 2.1 接口清单 `接口 \| 方法 \| 路径 \| 请求体 \| 响应体 \| 错误码`；2.2 请求流程 sequenceDiagram；2.3 服务实现 `服务/模块 \| 依赖 \| 文件路径 \| 核心方法` |
| §3 数据模型 | 3.1 存储结构 `Key/表/集合 \| 类型 \| 默认值 \| 读频率 \| 写频率 \| 说明`；3.2 数据迁移 `版本 \| 变更 \| 迁移策略` |
| §4 安全约束 | `# \| 威胁 \| 信任边界 \| 缓解措施 \| 优先级`（套用 Security 公式） |
| §5 性能与限制 | `维度 \| 约束 \| 应对` |
| §6 评审清单 | 7 项 ✅/❌：权限最小化/通信对齐/存储兼容/API 鉴权/无硬编码密钥/无误用长连接/输入校验完整 |

### F.story.03 — 03-前端技术评审.md

**公式**: `meta + nav + 组件架构 + 状态 + 交互 + 样式 + DOM + 依赖 + 评审清单`

| 章节 | 表头/内容 |
|------|----------|
| §1 组件架构 | 1.1 组件树 mermaid；1.2 新增/变更 `组件 \| 类型 \| 文件 \| 注册路径 \| 变更`；1.3 接口 `组件 \| Props \| Events \| Expose` |
| §2 状态管理 | 2.1 状态定义 `Store/State \| 文件 \| 状态字段 \| 使用组件` + 状态层 mermaid；2.2 状态流向 mermaid + `数据流 \| 触发源 \| 状态变更 \| 消费方` |
| §3 交互设计 | 3.1 用户操作流 mermaid；3.2 视图状态矩阵 `视图 \| 正常 \| 加载 \| 空 \| 错误 \| 禁用`；3.3 动画 `元素 \| 类型 \| 时长 \| 触发条件` |
| §4 样式方案 | 4.1 策略 `场景 \| 方案 \| 说明`（覆盖布局/主题/动画/隔离）；4.2 新增样式文件 `文件 \| 用途 \| 加载方式` |
| §5 DOM 与事件 | 5.1 挂载点 `组件 \| 容器 \| 创建方式 \| 生命周期`；5.2 事件 `事件 \| 监听方式 \| 处理逻辑 \| 清理时机` |
| §6 依赖与加载 | 6.1 加载顺序 fenced 块 + `新增文件 \| 插入位置 \| 依赖上游`；6.2 命名空间 `文件 \| 注册到 \| 类型` |
| §7 评审清单 | 8 项 ✅/❌：组件命名空间/资源注册/状态约定/样式隔离/事件清理/加载顺序/模块语法/样式文件注册 |

### F.story.04 — 04-测试用例评审.md

**公式**: `meta + nav + Tester公式 + 测试范围 + 用例×4类 + 环境专项 + 测试环境 + 评审清单 + Gate A 交接信号`

| 章节 | 表头/内容 |
|------|----------|
| §1 测试范围 | 1.1 覆盖矩阵 `FP# \| 功能点 \| 正常 \| 边界 \| 异常 \| 回归 \| 覆盖率`；1.2 Gate 映射 `Gate \| 用例范围 \| 通过标准 \| 交接下游`；1.3 影响链覆盖 `影响点 \| 来源 \| 回归用例 \| 覆盖状态` |
| §2 测试用例 | 2.1~2.4 四张同构表 `ID \| Given \| When \| Then \| 关联 FP \| 优先级`；ID 前缀 TC-N/TC-B/TC-E/TC-R |
| §3 运行环境专项 | 生命周期/通信通道/存储与数据，表头 `ID \| Given \| When \| Then \| 优先级`，ID 前缀 TC-X |
| §4 测试环境 | `维度 \| 配置`：运行环境/部署方式/测试目标/数据准备 |
| §5 评审清单 | 7 项 ✅/❌：每 FP ≥3 类/Gate A 覆盖/回归与影响链一致/异常含恢复行为/环境专项覆盖/无外部依赖比 ≥60%/影响链每点 ≥1 回归 |
| §6 Gate A 交接信号 | `信号 \| 内容`：通过状态/P0 用例 ID/实现约束/验证命令 |

### F.story.05 — 05-后端实施报告.md

**公式**: `meta + nav + Reporter公式 + 实施总结 + 偏差 + P0审查 + 存储变更 + 性能观察 + 评审清单`

| 章节 | 表头/内容 |
|------|----------|
| §1 实施总结 | 1.1 交付文件 `文件 \| 变更类型 \| 行数 \| 对应任务`；1.2 实际接口 `接口 \| 方法 \| 路径 \| 与评审偏差 \| 说明`；1.3 通信通道 `通道 \| 与评审偏差 \| 说明` |
| §2 偏差记录 | `# \| 评审设计 \| 实际实现 \| 偏差原因 \| 影响 \| 优先级`；无偏差注明 |
| §3 P0 审查 | 3.1 模块审查 `模块 \| 文件 \| P0 数量 \| 清零 \| 审查时间`；3.2 安全 `# \| 威胁 \| 缓解措施 \| 状态` |
| §4 存储变更 | `Key/表 \| 变更类型 \| 与评审偏差 \| 迁移验证` |
| §5 性能观察 | `维度 \| 观察 \| 与评审预期` |
| §6 评审清单 | 6 项 ✅/❌ |

### F.story.06 — 06-前端实施报告.md

**公式**: `meta + nav + Reporter公式 + 实施总结 + 偏差 + P0审查 + 样式隔离 + 依赖加载 + 评审清单`

| 章节 | 表头/内容 |
|------|----------|
| §1 实施总结 | 1.1 交付文件；1.2 实际组件 `组件 \| 注册路径 \| 与评审偏差 \| 说明`；1.3 状态管理 `Store/State \| 与评审偏差 \| 说明` |
| §2 偏差记录 | 同 05 §2 |
| §3 P0 审查 | `模块 \| 文件 \| P0 数量 \| 清零 \| 审查时间` |
| §4 样式与隔离 | `文件 \| 隔离方式 \| 与评审偏差` |
| §5 依赖与加载 | 5.1 注册表变更 `变更类型 \| 具体变更`；5.2 加载顺序验证 fenced 块 |
| §6 评审清单 | 5 项 ✅/❌ |

### F.story.07 — 07-测试用例报告.md

**公式**: `meta + nav + Reporter公式 + 测试环境 + 冒烟 + 回归 + 环境专项 + 已知问题 + Gate B 评估 + 评审清单`

| 章节 | 表头/内容 |
|------|----------|
| §1 测试环境 | `维度 \| 配置`：运行环境/部署方式/测试目标/数据状态/分支/环境快照(commit hash) |
| §2 冒烟 | 2.1 执行结果 `ID \| Given \| When \| Then \| 结果(✅/❌) \| 备注`；2.2 汇总 `指标 \| 数值`：总用例/通过/失败/P0 通过率/P1 通过率 |
| §3 回归 | `ID \| Given \| When \| Then \| 结果 \| 关联模块` |
| §4 环境专项验证 | `ID \| 场景 \| Given \| When \| Then \| 结果 \| 备注` |
| §5 已知问题 | `# \| 用例 ID \| Given \| When \| Then(实际) \| 优先级 \| 修复轮次 \| 状态` |
| §6 Gate B 评估 | `指标 \| 要求 \| 实际 \| 结果`：P0 100%/P1 ≥80%/P0 已知=0/修复轮次 ≤2 |
| §7 评审清单 | 6 项 ✅/❌ |

### F.story.08 — 08-自改进复盘.md

**公式**: `meta + nav + Self-Improve公式 + 基线校准 + 观察 + 诊断 + 改进 + 经验沉淀 + 评审清单`

| 章节 | 表头/内容 |
|------|----------|
| §0 基线校准 | `基线文件 \| 关键条款 \| 本次执行适用性 \| 偏差` 覆盖 CLAUDE.md / project-profile.json / 5 个 rules / 3 个 agents |
| §1 观察 | 1.1 时间线 `阶段 \| 开始 \| 结束 \| 耗时 \| Agent`（9 阶段）；1.2 质量快照 `指标 \| 本故事 \| 项目均值 \| 偏差`；1.3 关键事件 `# \| 事件 \| 阶段 \| 影响 \| 经验` |
| §2 诊断 | 2.1 诊断决策表 D1~D5 `规则 \| 触发条件 \| 本故事值 \| 触发? \| 根因假设`；2.2 六维评估 `维度 \| 评级 \| 证据 \| 说明`（耦合/稳定性/扩展性/可测试性/安全边界/依赖方向）；2.3 工流趋势 `指标 \| 本 \| 上 \| 趋势 \| 说明` |
| §3 改进 | 3.1 改进清单 `# \| 类别 \| 优先级 \| 改进动作 \| 诊断来源 \| 预期效果 \| 状态`；3.2 架构演进 `# \| 优先级 \| 变更 \| 六维来源 \| 时段 \| 状态`；3.3 提案同步 `提案ID \| 标题 \| 优先级 \| 状态 \| 效果评估` |
| §4 经验沉淀 | `# \| 经验 \| 类别 \| 来源阶段 \| 适用范围` |
| §5 评审清单 | 8 项（从 §0 到 §L 闭合） |

---

## 参考文档公式（docs/{文档类}/{project}/{resource}/）

四类参考文档统一结构：00-索引（导航入口）+ 01~04 分层（查阅→理解→审查→验证）。

### F.ref.00 — 00-索引（通用骨架）

**公式**: `meta + 身份卡片 + 角色阅读路径 + 场景阅读路径 + 文件清单 + 关联资源`

| 章节 | 内容 |
|------|------|
| 身份卡片 | `字段 \| 值`：资源名/路径/一句话职责/项目/技术栈或协议（按文档类） |
| 角色阅读路径 | `角色 \| 路径 \| 预计时间`，每条路径 ≤3 步 |
| 场景阅读路径 | `场景 \| 路径`：「调用/修改/排查」三类 |
| 文件清单 | `文件 \| 定位 \| 预计阅读`：01~04 各一行 |
| 关联资源 | 跨文档类引用：组件↔接口↔页面↔领域↔故事 |

**00 不含导航块、不含技术细节**。

### F.ref.section — 01~04 通用结构

每份 01~04 文件 = `meta + nav-头 + 一句话定位 + §1~§N 章节 + nav-尾`

「一句话定位」按递进:
- 01 = API 参考手册（查阅型）
- 02 = 架构蓝图/数据字典（理解型）
- 03 = 视觉规范/安全白皮书（审查型）
- 04 = 用户手册/集成手册/操作手册（验证型）

### F.ref.component — 组件文档（5 文件）

| 文件 | 章节骨架 |
|------|---------|
| 00-索引 | 身份卡片字段：组件名/文件路径/一句话职责/项目/技术栈/组件类型 |
| 01-组件概述 | §1 组件身份(`字段\|值`)、§2 接口契约(Props/Events/Expose 三表)、§3 使用示例(最小+典型代码块)、§4 版本兼容、§5 关联索引 |
| 02-状态与依赖 | §1 状态全景(`字段\|类型\|初始值\|来源\|分类\|持久化`)、§2 状态流图 mermaid flowchart、§3 子组件依赖 mermaid graph + 透传表、§4 外部引用(Composables/Store/3rd-party 三表)、§5 生命周期(`阶段\|操作\|说明`) |
| 03-样式与交互 | §1 样式策略(`场景\|方案\|说明`)、§2 视觉状态矩阵(`区域\|正常\|加载\|空\|错误\|禁用`)、§3 动画过渡、§4 DOM 与事件(挂载+事件清理)、§5 命名空间 |
| 04-操作场景 | §1 总览(`#\|场景\|触发角色\|前置条件\|覆盖 Props/Events`) + 状态过渡 mermaid、§2 正常场景(每场景 Given/When/Then 表)、§3 边界、§4 异常 |

### F.ref.api — 接口文档（5 文件）

| 文件 | 章节骨架 |
|------|---------|
| 00-索引 | 身份卡片字段：资源名/Base URL/职责/项目/协议(REST/GraphQL/gRPC/WebSocket) |
| 01-接口概述 | §1 服务身份、§2 端点清单(`#\|方法\|路径\|功能\|认证\|限流`)、§3 契约详情(每端点请求/响应/错误码 三块)、§4 序列图 sequenceDiagram、§5 版本兼容、§6 关联索引 |
| 02-数据模型 | §1 DTO(`字段\|类型\|必填\|校验\|示例`)、§2 ORM(`字段\|DB类型\|约束\|索引\|说明`) + erDiagram、§3 存储结构、§4 数据迁移、§5 容量策略 |
| 03-中间件与安全 | §1 中间件链路 flowchart + 表、§2 认证策略、§3 授权策略 + 端点权限矩阵、§4 输入校验、§5 威胁模型(套 Security 公式)、§6 错误码映射、§7 限流与防护 |
| 04-操作场景 | §1 总览、§2 正常调用(每场景 sequenceDiagram + 后置效果)、§3 集成场景(跨服务 sequenceDiagram + 超时/重试/补偿)、§4 异常场景、§5 性能约束 |

### F.ref.page — 页面文档（5 文件）

| 文件 | 章节骨架 |
|------|---------|
| 00-索引 | 身份卡片字段：页面名/路由/职责/项目/布局/页面类型 |
| 01-页面概述 | §1 页面身份(路由/路由名/布局/类型/路径/权限)、§2 数据注入(`数据源\|注入方式\|字段\|时机`)、§3 路由参数、§4 版本兼容、§5 关联索引 |
| 02-组件编排 | §1 组件树 mermaid、§2 组件清单(`组件\|路径\|Props透传\|职责`)、§3 组件间通信(Props/Event/Store/EventBus)、§4 懒加载、§5 命名空间 |
| 03-交互流程 | §1 页面状态流 stateDiagram-v2 + 状态说明表、§2 路由跳转(页内+跨页)、§3 模态/抽屉/通知、§4 表单交互、§5 错误与空态 |
| 04-操作场景 | §1 总览、§2 正常操作(Given/When/Then)、§3 边界(权限/网络/并发/大数据)、§4 异常(API 超时/校验失败/会话过期) |

### F.ref.domain — 领域模型（5 文件）

| 文件 | 章节骨架 |
|------|---------|
| 00-索引 | 身份卡片字段：领域名/限界上下文/职责/项目/核心实体 |
| 01-领域概述 | §1 领域边界(核心概念表 + 范围内/外列表)、§2 限界上下文(`字段\|值`)、§3 上下文映射 mermaid + 关系表、§4 上下游依赖(双表)、§5 关联索引 |
| 02-实体模型 | §1 实体清单(`实体\|类型\|标识符\|生命周期\|路径`)、§2 值对象、§3 聚合根(边界+一致性规则+不变式)、§4 关联关系 erDiagram + 表、§5 持久化映射 |
| 03-领域服务 | §1 服务清单(`服务\|职责\|依赖\|副作用\|路径`)、§2 领域事件(`事件\|触发\|payload\|消费者`) + flowchart、§3 仓储接口、§4 工厂、§5 领域规则 |
| 04-操作场景 | §1 总览、§2 领域内操作(Given/When/Then + sequenceDiagram)、§3 跨领域协作 sequenceDiagram + 协作表、§4 异常(规则违反/并发冲突/补偿/最终一致延迟) |

---

## 补充文档公式（按需，编号 10~19）

pm 在文档生成阶段按 docs.md §「补充文档规则」决策表选择。无匹配条件不生成。

| 编号 | 文档 | 主要章节 |
|------|------|---------|
| 10 | 页面设计 / API 契约 | 取决于触发条件，沿用 03/02 评审结构 |
| 11 | 数据迁移 | 旧/新结构对比、迁移脚本、回滚、验证 |
| 12 | 集成方案 | 集成点、契约、错误处理、安全考量 |
| 13 | 权限模型 | RBAC/ABAC、角色矩阵、资源归属 |
| 14 | 性能基准 | 指标、基线、优化策略 |
| 15 | 消息通道 | 通道、Payload、消费者、错误处理 |
| 16 | 模块接口 | 跨故事共享接口、版本、兼容性 |

补充文档无固定模板，按所对应主线文档章节风格 ad-hoc 生成。

---

## 通知/记忆文档（自动生成）

| 文件 | 生成方 | 公式 |
|------|--------|------|
| `00-消息通知列表.md` | wework-bot | 列表追加，每条含时间戳+类型+payload，不入库审查 |
| `.memory/execution-memory.jsonl` | rui-state.js | 追加 JSON，字段见 [data.md](./data.md) |
| `.memory/rui-state.json` | rui-state.js | 单对象覆盖写，字段见 [data.md](./data.md) |
| `.improvement/proposals.jsonl` | self-improve.js | 追加 JSON，字段见 [data.md](./data.md) |

---

## 使用约定

1. **生成时**: 按文件公式逐章节产出，不引用 templates/——不存在了
2. **裁剪时**: T1 仅写变更章节；T2 同步影响章节；T3 全文重生
3. **校验时**: F.meta + F.nav 占位符必须替换；表列必须齐；mermaid 必须可渲染
4. **扩展时**: 新文档类型在本文件追加新公式块，不要再创建模板目录
