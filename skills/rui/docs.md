# 文档规范

## 文档分层

```
docs/
├── 组件文档/                  ← --from-code 前端探索生成（项目级参考文档）
│   ├── {project}-{component}.md ← 单个组件的 Props/Events/状态/依赖/样式全文档
│   └── ...
├── 接口文档/                  ← --from-code 后端探索生成（项目级参考文档）
│   ├── {project}-{resource}-api.md ← 单个资源的端点/契约/中间件/数据模型全文档
│   └── ...
└── 故事任务面板/              ← rui doc/code 生成（故事级执行文档）
    └── <project>-<name>/
        ├── 01-故事任务.md      ← 引用 → ../../组件文档/{project}-{component}.md
        └── ...
```

**项目级参考文档**（`docs/组件文档/`、`docs/接口文档/`）：由 `--from-code` 空 req 探索生成。描述代码的当前状态（what is），不包含任务/AC/设计决策。供故事目录下的 01~08 引用。

**故事级执行文档**（`docs/故事任务面板/<project>-<name>/`）：由 `rui doc`/`rui code` 生成。描述要做什么、怎么做、做了什么（what to do / what was done）。引用项目级参考文档作为事实基线。

### 引用格式

故事文档引用项目级参考文档时使用相对路径：

```markdown
详见 [UserTable 组件文档](../../组件文档/YiWeb-user-table.md) §3 状态流。
API 契约参见 [用户 API 文档](../../接口文档/YiWeb-user-api.md) §2 端点清单。
```

## 故事拆分

pm 收到需求后按决策树判断是否拆分：

```
需求 → 单一场景/单一角色? → 是 → 一个故事
                    → 否 → 可独立验证的子需求? → 逐个子需求拆为独立故事
                    → 否 → 一个故事 + 明确范围边界
```

### 拆分信号

| 信号 | 说明 | 处理 |
|------|------|------|
| 多角色 | 需求涉及 ≥2 个独立角色（管理员/终端用户/开发者） | 按角色拆分 |
| 多入口 | 需求涉及 ≥2 个独立入口（Web/API/CLI/后台） | 按入口拆分 |
| 可独立交付 | 子需求可独立上线并产生用户价值 | 拆为独立故事 |
| 跨边界 | 需求跨越前后端且任一端复杂度 > 3 模块 | 前端故事 + 后端故事 |
| 无拆分信号 | 单一场景、单一角色、不可再分 | 一个故事 |

### 拆分约束

- 每个故事必须有独立的 AC（可独立验证）
- 故事间依赖显式标注在 §1 依赖字段
- 拆分后逐故事串行，先完成的故事先验证
- 禁止过度拆分：一个函数/一个 API 不构成独立故事

### 故事目录命名

| 故事类型 | 命名格式 | 示例 |
|---------|---------|------|
| 功能故事 | `<project>-<feature>` | `YiWeb-user-login`、`YiAdmin-role-mgmt` |
| 重构故事 | `<project>-<scope>-refactor` | `YiWeb-auth-refactor` |
| 修复故事 | `<project>-<scope>-fix` | `YiWeb-login-timeout-fix` |

规则：project 为大驼峰（如 `YiWeb`），story 为 kebab-case（如 `user-login`）。总长度 ≤ 64 字符。

## 故事目录结构

按项目类型自动选择目录骨架。pm 在文档生成阶段根据项目类型和故事内容决定创建哪些文件。

### 全栈故事

```
docs/故事任务面板/<project>-<name>/
├── 01-故事任务.md              ← 唯一真相源
├── 02-后端技术评审.md           ← 服务架构/API/数据模型/安全
├── 03-前端技术评审.md           ← 组件树/状态流/样式/交互
├── 04-测试用例评审.md           ← 覆盖矩阵/Gate 映射/用例
├── 05-后端实施报告.md           ← 交付文件/偏差/审查/存储
├── 06-前端实施报告.md           ← 交付文件/偏差/审查/性能
├── 07-测试用例报告.md           ← 执行结果/覆盖率/Gate 结论
├── 08-自改进复盘.md             ← 六维评估/改进项/经验沉淀
├── 00-消息通知列表.md           ← wework-bot 自动生成（不入库审查）
├── 10-{领域}.md                 ← 按需：补充文档（领域建模、专项设计等）
├── 1x-{专题}.md                 ← 按需：专题文档（迁移方案、集成方案等）
├── .improvement/proposals.jsonl  ← 自改进提案（追加写入）
└── .memory/
    ├── execution-memory.jsonl    ← 执行记忆（追加写入）
    └── rui-state.json            ← 管线状态（单对象覆盖写入）
```

### 纯前端故事

后端不涉及任何服务端改动时（纯 UI/组件/样式/交互），省略后端文件：

```
docs/故事任务面板/<project>-<name>/
├── 01-故事任务.md
├── 03-前端技术评审.md           ← 必选
├── 04-测试用例评审.md
├── 06-前端实施报告.md           ← 必选（前端项目必选前端实施报告）
├── 07-测试用例报告.md
├── 08-自改进复盘.md
├── 00-消息通知列表.md
├── .improvement/proposals.jsonl
└── .memory/
```

省略：`02-后端技术评审.md`、`05-后端实施报告.md`。

### 纯后端故事

前端不涉及任何 UI 改动时（纯 API/数据/服务/脚本），省略前端文件：

```
docs/故事任务面板/<project>-<name>/
├── 01-故事任务.md
├── 02-后端技术评审.md           ← 必选
├── 04-测试用例评审.md
├── 05-后端实施报告.md           ← 必选
├── 07-测试用例报告.md
├── 08-自改进复盘.md
├── 00-消息通知列表.md
├── .improvement/proposals.jsonl
└── .memory/
```

省略：`03-前端技术评审.md`、`06-前端实施报告.md`。

## 文件创建生命周期

```
需求解析 → 自适应规划 → 影响分析 → 架构设计 → 文档生成 → 预检 → 测试先行 → 实现 → 验证 → 自改进 → 交付
                                                      │                │         │         │        │
                                                      ▼                ▼         ▼         ▼        ▼
                                                   01 创建       02/03/04   05/06/07   08    00(通知)
                                                   故事任务        创建       创建      创建    创建
```

| 阶段 | 创建文件 | 负责人 | 条件 |
|------|---------|--------|------|
| 文档生成 | `01-故事任务.md` | pm | 必创建 |
| 文档生成(架构设计后) | `02-后端技术评审.md` | coder + security | 涉及后端 |
| 文档生成(架构设计后) | `03-前端技术评审.md` | coder | 涉及前端 |
| 文档生成(架构设计后) | `04-测试用例评审.md` | tester | 必创建 |
| 文档生成(补充) | `10-{领域}.md` / `1x-{专题}.md` | pm 决策 | 见补充文档规则 |
| 验证 | `05-后端实施报告.md` | coder | 涉及后端且有 02 |
| 验证 | `06-前端实施报告.md` | coder | 涉及前端且有 03 |
| 验证 | `07-测试用例报告.md` | tester | 必创建 |
| 自改进 | `08-自改进复盘.md` | pm + reporter | 必创建 |
| 交付 | `00-消息通知列表.md` | wework-bot | 自动生成 |
| 实现→验证 | `.memory/execution-memory.jsonl` | 自动 | 追加写入 |
| 全阶段 | `.memory/rui-state.json` | 自动 | 每次阶段变更覆盖写入 |
| 自改进 | `.improvement/proposals.jsonl` | self-improve | 有提案时追加写入 |

> **关键约束**：
> - 01 是唯一真相源 — 所有其他文件的引用必须最终追溯到 01
> - 02/03/04 在文档生成阶段创建，05/06/07 在验证阶段创建 — 不可提前
> - `.memory/` 和 `.improvement/` 由脚本自动管理，人工不编辑
> - 补充文档（10/1x）在文档生成阶段由 pm 按决策表判断是否创建

## 完整度判定

`list.js` 按文件存在性判定故事状态。判定逻辑以项目类型为基准 — 纯前端故事不要求后端文件，反之亦然：

| 状态 | 条件 |
|------|------|
| `not_started` | 01 不存在 |
| `docs_in_progress` | 01 存在，必选文件有缺失 |
| `docs_done` | 所有必选文档文件存在 |
| `code_in_progress` | 文档齐全，存在部分实施报告 |
| `code_done` | 所有必选文件存在 + 08 存在 |
| `blocked` | rui-state.json 中 blocked=true |

## --from-code 自主探索模式

`/rui doc --from-code` 不传 req 时，pm 按项目类型差异化探索源码：

### 通用流程

1. 检测项目类型（前端/后端/全栈），选择对应探索策略
2. 扫描项目目录，识别可独立文档化的单元
3. 每个候选输出：推荐名称(kebab-case)、覆盖范围、源码证据（Level A 路径列表）、优先级
4. 按优先级排序（有源码无文档 > 有文档过时 > 其他）
5. 用户选择后，生成**项目级参考文档**写入 `docs/组件文档/` 或 `docs/接口文档/`
6. 随后自动推荐可基于参考文档创建的故事

### 前端项目：组件发现 → docs/组件文档/

| 步骤 | 操作 | 产出 |
|------|------|------|
| 1. 组件扫描 | 搜索 `.vue` / `.jsx` / `.tsx` / `.svelte` 文件，排除 `node_modules` | 组件文件列表 |
| 2. 接口提取 | 解析每个组件的 Props/Events/Expose（Vue）或 Props/回调（React） | 组件契约表（参数名、类型、必要性） |
| 3. 依赖分析 | 识别组件子组件、composables/hooks、store 引用 | 组件依赖图 |
| 4. 状态追踪 | 提取 data/reactive/ref/useState/useReducer，标注状态变更路径 | 状态流图 |
| 5. 样式检查 | 识别样式隔离方案（Scoped/CSS Modules/Shadow DOM/Tailwind） | 样式策略标注 |
| 6. 生成参考文档 | 每个选中组件生成 `docs/组件文档/{project}-{component}.md` | 推荐列表（P0: 核心业务组件无文档 > P1: 普通组件无文档 > P2: 已有过时文档） |



### 后端项目：接口发现 → docs/接口文档/

| 步骤 | 操作 | 产出 |
|------|------|------|
| 1. 路由扫描 | 搜索路由/控制器文件，匹配 `@Route` / `@RequestMapping` / `app.get` / `router.get` / `http.HandleFunc` 等 | API 端点列表 |
| 2. 契约提取 | 每个端点：HTTP 方法、路径、请求体/查询参数 schema、响应体 schema、状态码 | API 契约表 |
| 3. 中间件链 | 识别认证/授权/限流/日志中间件 | 中间件链路图 |
| 4. 数据模型 | 提取请求/响应 DTO、ORM 模型、数据库迁移脚本 | 数据模型依赖 |
| 5. 错误处理 | 提取错误码定义、异常处理策略 | 错误码表 |
| 6. 生成参考文档 | 每个选中接口组生成 `docs/接口文档/{project}-{resource}-api.md` | 推荐列表（P0: 核心 API 无文档 > P1: 普通 API 无文档 > P2: 已有过时文档） |



### 全栈项目

同时运行前端和后端探索策略，分别列出推荐。用户可独立选择前端或后端范围，也可全选。

### 传 req 时

`/rui doc --from-code <req>` 跳过探索，直接按 req 限定范围反推故事，不输出推荐列表。

## 故事板章节

01-故事任务.md 的完整章节结构：

| 章节 | 负责人 | 公式 | 内容 | 必选 |
|------|--------|------|------|------|
| 元信息 | pm | — | version/timestamp/model/branch/角色公式速查 | ✓ |
| §1 Story | pm | 作为...我想要...以便... | 角色场景、价值、范围边界、依赖、范围外 | ✓ |
| §1.1 User Operations | tester | Given→When→Then | 用户操作路径 + UI 交互流程图 | 涉及 UI 时 |
| §2 Requirements | pm | 输入→输出→错误行为 | 功能点表、业务规则表、数据约束表 | ✓ |
| §3 Design | coder + security | 模块→接口→数据流 / 威胁→信任边界→缓解 | 设计概述 + 决策表(仅 WHY 不写 HOW) | ✓ |
| §4 Tasks | pm + all | — | 任务拆解表(含依赖/交付物/Agent/门禁) + 依赖图 | ✓ |
| §5 Acceptance Criteria | tester | Given→When→Then | AC 表(含 Gate A/B 标注)，每个 AC 可独立验证 | ✓ |
| §6 .claude 改进清单 | pm | 观察→诊断→改进 | skill/agent/rule/script/config 改进项 | 可选 |
| §7 系统架构演进任务 | pm | 观察→诊断→改进 | 近期/中期/远期架构演进建议 | 可选 |
| §L 自我改进循环 | self-improve | 观察→诊断→改进 | 数据驱动改进清单(每次 rui 完成追加) | 可选 |

> §6 和 §7 由 pm 在文档生成阶段判断是否填写。无改进建议或架构演进建议时可省略。§L 由 self-improve agent 在自改进阶段追加。

## 补充文档规则

pm 在文档生成阶段按决策表判断。补充文档编号规则：`10-{领域}.md`（领域级补充），`1x-{专题}.md`（专题级补充，x = 1..9）。

| 条件 | 生成文档 | 负责人 | 编号 |
|------|---------|--------|------|
| §1.1 涉及 UI 改造 | 页面设计文档 | coder | `10-页面设计.md` |
| §2 涉及新增/修改 API | API 契约文档 | coder | `10-API契约.md` |
| §2 涉及数据存储变更 | 数据迁移方案 | coder | `11-数据迁移.md` |
| 第三方服务集成 | 集成方案 | coder + security | `12-集成方案.md` |
| 新权限控制 | 权限模型 | security | `13-权限模型.md` |
| 性能敏感路径 | 性能基准 | coder | `14-性能基准.md` |
| 新增/变更消息队列 | 消息通道文档 | coder | `15-消息通道.md` |
| 跨故事共享模块 | 模块接口文档 | coder | `16-模块接口.md` |

无匹配条件不生成补充文档。有模板优先用模板，无模板按已有模式 ad-hoc 生成。

## 基线文件

| 文件 | 负责人 | 产出阶段 | 纯前端 | 纯后端 | 全栈 |
|------|--------|---------|--------|--------|------|
| 01-故事任务.md | pm | 文档生成 | ✓ | ✓ | ✓ |
| 02-后端技术评审.md | coder + security | 文档生成(架构设计后) | — | ✓ | ✓ |
| 03-前端技术评审.md | coder | 文档生成(架构设计后) | ✓ | — | ✓ |
| 04-测试用例评审.md | tester | 文档生成(架构设计后) | ✓ | ✓ | ✓ |
| 05-后端实施报告.md | coder | 验证 | — | ✓ | ✓ |
| 06-前端实施报告.md | coder | 验证 | ✓ | — | ✓ |
| 07-测试用例报告.md | tester | 验证 | ✓ | ✓ | ✓ |
| 08-自改进复盘.md | pm + reporter | 自改进 | ✓ | ✓ | ✓ |

三份实施与测试报告交叉引用闭合，各报告评审清单全部 ✓ 方可通过 Gate B。
