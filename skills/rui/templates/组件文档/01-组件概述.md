# 组件概述: {ComponentName}

> **导航**: [← 00-索引](./00-索引.md) · [↑ 组件文档](../) · [02-状态与依赖 →](./02-状态与依赖.md)
> | v{version} | {YYYY-MM-DD} | {模型} | 🌿 {branch} |

> **定位**: API 参考手册 — 查阅型。开发者想查某个 prop 的类型，30 秒内定位。

---

## §1 组件身份

| 字段 | 值 |
|------|---|
| 文件路径 | `{path}` |
| 注册路径/命名空间 | `{Namespace}.{Name}` |
| 挂载容器 | `{selector / mount point}` |
| 组件类型 | {展示 / 容器 / 布局} |
| 模块封装 | {IIFE / ES Module / SFC / ...} |

---

## §2 接口契约

### Props (输入)

| 名称 | 类型 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| `{propName}` | `{Type}` | `{default}` | ✓/— | {说明} |

### Events (输出)

| 名称 | 参数 | 触发时机 |
|------|------|---------|
| `{eventName}` | `{params: Type}` | {触发条件} |

### Expose (暴露方法)

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `{method()}` | `{params}` | `{Type}` | {用途} |

---

## §3 使用示例

### 最小可用

```{lang}
{最小可用示例代码}
```

### 典型用法

```{lang}
{典型用法代码，展示常用 Props 组合}
```

---

## §4 版本兼容

| 版本 | 变更 | 迁移指引 |
|------|------|---------|
| v{N} | {Breaking Change 描述} | {迁移步骤} |

> 无 Breaking Changes 时注明"无"。

---

## §5 关联索引

- 状态与依赖: [02-状态与依赖.md](./02-状态与依赖.md)
- 样式与交互: [03-样式与交互.md](./03-样式与交互.md)
- 操作场景: [04-操作场景.md](./04-操作场景.md)

> **导航**: [← 00-索引](./00-索引.md) · [02-状态与依赖 →](./02-状态与依赖.md)
