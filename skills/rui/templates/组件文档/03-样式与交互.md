# 样式与交互: {ComponentName}

> **导航**: [← 02-状态与依赖](./02-状态与依赖.md) · [↑ 00-索引](./00-索引.md) · [04-操作场景 →](./04-操作场景.md)
> | v{version} | {YYYY-MM-DD} | {模型} | 🌿 {branch} |

> **定位**: 视觉规范 — 审查型。设计师核对视觉状态，开发者查样式隔离规则。

---

## §1 样式策略

| 场景 | 方案 | 说明 |
|------|------|------|
| 隔离方式 | {Scoped / CSS Modules / Shadow DOM / Tailwind / 前缀} | {具体策略} |
| 设计 Token | `{token 路径}` | {变量引用方式} |
| 禁止事项 | {全局 reset / 裸标签选择器 / !important} | — |

---

## §2 视觉状态矩阵

| 视图区域 | 正常 | 加载 | 空 | 错误 | 禁用 |
|---------|------|------|----|------|------|
| {区域A} | {正常态} | {loading 方式} | {空态文案+引导} | {错误提示+恢复} | — |
| {区域B} | {正常态} | {loading 方式} | {空态文案} | {错误消息} | {禁用条件} |

---

## §3 动画过渡

| 元素 | 动画类型 | 时长 | 缓动 | 触发条件 |
|------|---------|------|------|---------|
| `{element}` | {CSS transition / JS animation} | {ms} | {ease/linear/...} | {条件} |

> 无动画时注明"无动画"。

---

## §4 DOM 与事件

### 挂载

| 组件 | 挂载容器 | 创建方式 | 生命周期 |
|------|---------|---------|---------|
| `{Name}` | `{selector}` | {createElement / mount / inject} | {挂载/卸载时机} |

### 事件处理

| 事件 | 监听目标 | 处理逻辑 | 清理时机 |
|------|---------|---------|---------|
| `{eventType}` | `{target}` | {处理逻辑} | {卸载时 / 路由切换时} |

---

## §5 命名空间

| 字段 | 值 |
|------|---|
| 注册命名空间 | `{Namespace}.{Module}` |
| 样式前缀 | `{prefix-}` |
| 加载顺序位置 | {在 X 之后，Y 之前} |

> **导航**: [← 02-状态与依赖](./02-状态与依赖.md) · [04-操作场景 →](./04-操作场景.md)
