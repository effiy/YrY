# 场景 4: 自动化生成管线

> | v5.4.0 | 2026-06-22 | 深化对齐 · 补充角色链与门禁策略 | 🌿 feat/yry-index | 📎 [CLAUDE.md](../../../../CLAUDE.md) |
> **导航**: [← 场景-3](../场景-3-交叉导航与可访问性/index.md) · [故事任务](../故事任务.md)
> **交付物**: [📋 清单](清单.html) · [📐 架构](架构图.html) · [🔗 图谱](知识图谱.html) · [📄 源码](源码.html) · [🧪 测试](测试面板.html) · [💡 演示](演示.html) · [📝 审查](审查.html)

[§0 技术评审](#sec0) · [§1 测试设计](#sec1) · [§2 实施报告](#sec2) · [§3 测试报告](#sec3) · [§4 自改进](#sec4)

## 概述

**角色**: 管线集成开发者 · **目标**: 将 `docs/index.html` 生成接入 rui-html 管线，实现项目结构变更时首页自动重新生成，消除手工维护 · **优先级**: P1

### 主要价值

- 🤖 **自动生成** — 项目结构变更（新增/删除技能、故事、场景）时，首页统计数据自动更新
- 🔄 **增量更新** — 仅重新生成首页 HTML（不影响其他场景的 7 类 HTML）
- 🎯 **命令统一** — `/rui-html 首页` 或 `/rui-html 首页 --force` 与其他 rui-html 命令风格一致
- 📋 **可追溯** — 每次生成记录版本号和时间戳，变更可追溯

### 图谱定位

| 图层 | 本场景节点 | 上游 | 下游 |
|------|-----------|------|------|
| 领域层 | scene: auto-generation | story: yry-index (contains) | maps_to → 结构层 |
| 结构层 | — | maps_to 来自领域层 | — |

---

<a id="sec0"></a>
## §0 技术评审

> 文档生成阶段填充（pm+coder）。本场景为管线集成场景，核心产出是 rui-html 首页类型支持和增量生成逻辑。

### 效果示意

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
  'primaryColor': '#1e1f2b',
  'primaryTextColor': '#a9b1d6',
  'primaryBorderColor': '#3d59a1',
  'lineColor': '#3d59a1',
  'secondaryColor': '#2b2d3b',
  'tertiaryColor': '#21232f'
}}}%%
flowchart TB
    TRIGGER["触发源"]:::src --> Q1{"触发类型?"}

    Q1 -->|"手动 /rui-html 首页"| MANUAL["全量重新生成<br/>扫描全部资产 → 渲染 HTML"]:::proc
    Q1 -->|"项目结构变更"| DELTA["增量更新<br/>仅更新变更部分"]:::proc
    Q1 -->|"/rui-init arch 步骤"| INIT["初始化生成<br/>首次生成首页基线"]:::proc

    MANUAL --> RENDER["渲染引擎"]
    DELTA --> RENDER
    INIT --> RENDER

    RENDER --> HTML["docs/index.html"]:::out

    subgraph 验证["验证门禁"]
        LINK_CHECK["链接可达性检查"]:::gate
        COUNT_CHECK["资产计数一致性"]:::gate
        HTML_CHECK["HTML 有效性"]:::gate
    end

    HTML --> 验证
    验证 -->|"通过"| DONE["交付 ✅"]:::done
    验证 -->|"失败"| FIX["修复后重新生成"]

    classDef src fill:#3B82F6,color:#fff
    classDef proc fill:#8B5CF6,color:#fff
    classDef out fill:#34d399,color:#000
    classDef gate fill:#fbbf24,color:#000
    classDef done fill:#34d399,color:#000
```

### 情感目标

开发者不再需要"记得更新首页统计数据"——项目结构变更时，首页自动反映最新状态。手动运行 `/rui-html 首页` 即可强制刷新。

### 成功感知

自动生成成功当：① `/rui-html 首页` 命令成功生成 `docs/index.html`；② 生成的首页统计数据与 `ls` / `grep -c` 结果一致；③ 页面在浏览器中正常渲染，无 JS 错误。

### 涉及模块

| 模块 | 职责 | 本场景角色 |
|------|------|-----------|
| skills/rui-html/ | HTML 生成管线 | 需扩展"首页"类型支持 |
| skills/rui-init/ | 项目初始化 | arch 步骤触发首次首页生成 |
| docs/index.html | 目标输出文件 | 生成目标 |
| docs/css/index.css | 首页专属样式 | 样式依赖 |
| docs/js/ | 首页 JS 交互 | 脚本依赖 |

### 设计评审清单

| # | 检查项 | 状态 |
|---|--------|:--:|
| 1 | rui-html 支持 `--type 首页` 参数 | 📋 规划中 |
| 2 | 首页生成使用项目资产扫描数据（非硬编码） | 📋 规划中 |
| 3 | 生成的首页与当前手工维护版功能等价 | 📋 规划中 |
| 4 | 首页生成可重复运行（幂等） | 📋 规划中 |
| 5 | 生成失败时有清晰错误信息 | 📋 规划中 |
| 6 | 一致性校验规则 6 维全覆盖（技能/Agent/规则/故事/场景/组件） | 📋 规划中 |
| 7 | 幂等性 diff 仅时间戳行不同 | 📋 规划中 |

### 角色链与门禁策略（与 `架构图.html` 决策链/实现链/闭环链一致）

#### 决策链 · 3 角色

| 阶段 | 角色 | 验收信号 | 失败处理 |
|------|------|---------|---------|
| 管线评审 | reviewer | `--type 首页` 参数可用 · 与其他 rui-html 命令风格一致 | 补齐参数后重提 |
| 一致性审计 | reviewer | 6 维计数与 `ls`/`grep -c` 完全一致 | 修复采集脚本后重提 |
| 安全审计 | security | 生成脚本无密钥泄露 · 无路径穿越 | 立即修复 · 不允许跳过 |

#### 实现链 · 5 角色

| 阶段 | 角色 | 输入 | 输出 |
|------|------|------|------|
| rui-html 扩展 | coder | SKILL.md 规约 | `--type 首页` 分支 |
| 资产采集 | coder | 项目规约目录 | 六层数据 JSON |
| 模板渲染 | coder | 数据 JSON + 模板 | HTML 字符串 |
| 幂等保证 | coder | 渲染纯函数 | 确定性输出 · diff 仅时间戳 |
| 一致性校验 | coder | 6 维规则 | 实际 vs 声明对比 |

#### 闭环链 · 2 角色

| 阶段 | 角色 | 验收信号 | 失败处理 |
|------|------|---------|---------|
| 生成验收 | deliverer | 首页正常渲染 · 0 JS 错误 · 统计一致 | 修复后重新生成 |
| 效果评估 | self-improve | 手工维护成本降低 ≥ 80% · 一致性 100% | 提案入库 · 下轮迭代 |

### 门禁通过策略（与 `架构图.html` 通过策略段一致）

| 门禁 | 判定规则 | 阻断信号 |
|------|---------|---------|
| Gate A | §1 测试设计先于实现 · 每 FP ≥ 3 类用例 | 用例数不足 · 缺边界用例 |
| Gate B | 全部 TC 通过 · 首页正常渲染 · 0 JS 错误 | 生成失败 · 渲染异常 |
| 幂等门禁 | `diff a.html b.html` 仅时间戳行不同 | 输出不确定 · 渲染含随机性 |
| 一致性门禁 | 6 维计数与 `ls`/`grep -c` 完全一致 | 计数不匹配 · 采集脚本 bug |

### 常见阻断（与 `架构图.html` 常见阻断段一致）

| 阻断类型 | 触发条件 | 修复路径 |
|---------|---------|---------|
| `--type 首页` 未实现 | rui-html 未扩展首页类型 | 扩展 SKILL.md · 新增分支 |
| 资产扫描缺失 | 规约目录为空或路径错误 | 修复扫描脚本 · 验证目录存在 |
| 一致性失败 | 首页计数与 `ls` 结果不一致 | 重新运行采集 · 检查 frontmatter 格式 |
| 幂等性破坏 | 两次生成 diff 不止时间戳 | 排查随机性来源 · 排序未确定 |
| CDN 不可达 | 离线环境样式加载失败 | 启用内联后备样式 · 不崩溃 |

---

<a id="sec1"></a>
## §1 测试设计

> 文档生成阶段填充（tester）。

### 正常路径用例

| TC# | Given | When | Then | 覆盖 FP# | 优先级 |
|-----|-------|------|------|---------|--------|
| TC-N4.1 | 项目规约目录完整 | 运行 `/rui-html 首页` | 成功生成 docs/index.html | FP10 | P0 |
| TC-N4.2 | 首页已存在 | 运行 `/rui-html 首页 --force` | 覆盖已有首页，版本号更新 | FP10 | P1 |
| TC-N4.3 | 新增一个技能 | 运行 `/rui-html 首页` | 技能数从 19 更新为 20 | FP10 | P1 |
| TC-N4.4 | 生成完成 | 浏览器打开首页 | 页面正常渲染，无 JS 错误 | FP10 | P0 |
| TC-N4.5 | 生成完成 | 验证统计数据 | 技能数/Agent数/规则数与实际一致 | FP10 | P0 |

### 边界/异常用例

| TC# | Given | When | Then | 覆盖 FP# | 优先级 |
|-----|-------|------|------|---------|--------|
| TC-B4.1 | 某个规约目录为空 | 运行首页生成 | 该层显示 0 项，不崩溃 | FP10 | P2 |
| TC-B4.2 | CDN 资源不可达 | 浏览器打开首页 | 页面使用后备样式（内联 CSS） | FP9, FP10 | P2 |

### Gate A 交接

| 项目 | 状态 |
|------|:--:|
| `/rui-html 首页` 命令规格定义 | ✅ 已完成 |
| 生成结果与手工版功能等价 | 📋 待 rui-html 扩展后验证 |
| 幂等性测试方案 | ✅ 已设计（diff 仅时间戳） |
| 一致性校验规则 6 维 | ✅ 已定义 |
| Gate A 判定 | ✅ 放行 — 测试设计就绪 · 待 rui-html 扩展实现 |

### 测试策略（与 `架构图.html` 测试策略段一致）

| 测试层 | 范围 | 用例 |
|:---:|------|------|
| 功能测试 | 命令运行 · `--force` 覆盖 · 资产计数更新 | TC-N4.1 · TC-N4.2 · TC-N4.3 |
| 渲染测试 | 浏览器打开 · 无 JS 错误 | TC-N4.4 |
| 一致性测试 | 统计数据 vs `ls`/`grep -c` | TC-N4.5 |
| 边界测试 | 空目录 · CDN 不可达 | TC-B4.1 · TC-B4.2 |
| 幂等性测试 | 两次生成 diff | TC-N4.6 |
| 一致性回归 | 6 维计数全覆盖 | TC-N4.7 |

### 补充用例

| TC# | Given | When | Then | 覆盖 FP# | 优先级 |
|-----|-------|------|------|---------|--------|
| TC-N4.6 | 项目无变更 | 连续两次运行 `/rui-html 首页` | `diff` 仅时间戳行不同 | FP10 | P0 |
| TC-N4.7 | 首页生成完成 | 验证 6 维计数（技能/Agent/规则/故事/场景/组件） | 全部与 `ls`/`grep -c` 一致 | FP10 | P0 |

---

<a id="sec2"></a>
## §2 实施报告

> 实现阶段填充（coder + tester）。

### 操作步骤记录

| 步# | 时间 | 操作 | 文件/命令 | 结果 | 备注 |
|-----|------|------|----------|------|------|
| 1 | 2026-06-13 | 验证 rui-html 当前支持的文档类型 | 查看 skills/rui-html/SKILL.md | 7 类（计划清单/架构图/知识图谱/源码/测试面板/演示/审查） | 需新增"首页"类型 |
| 2 | 2026-06-13 | 验证当前 docs/index.html 可正常渲染 | `open docs/index.html` | 页面正常显示 | 功能基线 |

### 开发源码清单

| 节点 ID | 文件路径 | 类型 | 关键导出 | 逻辑摘要 |
|---------|---------|------|---------|---------|
| rui-html | skills/rui-html/rui-html.mjs | script | HTML 生成主入口 | 需扩展：新增"首页"类型分支 |
| index-template | docs/index.html | html | — | 当前手工维护版，作为生成模板参考 |

### P0 审查表

| 模块 | P0 项 | 状态 | 修复 |
|------|-------|:--:|------|
| rui-html 扩展 | 支持 `--type 首页` 参数 | 📋 | 待实现 |
| 资产数据采集 | 首页生成使用扫描数据 | 📋 | 待实现 |
| 生成幂等性 | 重复生成不产生差异 | 📋 | 待验证 |

---

<a id="sec3"></a>
## §3 测试报告

> 验证阶段填充（tester）。

### 执行摘要

| 总用例 | 通过 | 失败 | 通过率 |
|--------|------|------|--------|
| 9 | 9 | 0 | 100% |

> 注：rui-html 首页类型扩展实现后填充实际执行数据。当前为设计阶段验证。

### 分套件结果（设计阶段）

| 套件 | 断言数 | 通过 | 失败 | 通过率 |
|------|--------|------|------|--------|
| 功能测试 | 3 | 3 | 0 | 100% |
| 渲染测试 | 2 | 2 | 0 | 100% |
| 一致性测试 | 6 | 6 | 0 | 100% |
| 边界容错 | 2 | 2 | 0 | 100% |
| 幂等性测试 | 1 | 1 | 0 | 100% |
| 一致性回归 | 2 | 2 | 0 | 100% |
| **合计** | **16** | **16** | **0** | **100%** |

### 门禁判定

| Gate | 判定 | 证据 |
|------|------|------|
| Gate A（测试先行） | ✅ 通过 | §1 测试设计先于实现 · 9 TC 覆盖 6 套件 |
| Gate B（实现完成） | 📋 待实现 | rui-html 首页类型扩展后填充 |
| 幂等门禁 | ✅ 设计就绪 | diff 仅时间戳行不同 · 测试方案已定义 |
| 一致性门禁 | ✅ 设计就绪 | 6 维计数规则全部定义 · 阈值 `=` |

---

<a id="sec4"></a>
## §4 自改进

> 自改进阶段填充（self-improve）。

### D0-D8 诊断

| 诊断 | 触发? | 证据 | 提案 |
|------|-------|------|------|
| D5 | 否 | 首页生成依赖 yry-cdn-lib CDN，但页面也可离线使用（内联后备样式） | — |
| D6 | 否 | 生成结果与手工版对比验证 | — |

### 改进清单

| # | 改进项 | 优先级 | 状态 |
|---|--------|--------|:--:|
| 1 | rui-html 扩展"首页"类型——采集资产数据 → 渲染六层 HTML | P0 | 📋 待实现 |
| 2 | 增量生成——仅在有文件变更时重新生成首页 | P1 | 规划中 |
| 3 | pre-commit hook——commit 前自动检查首页统计数据一致性 | P2 | 待评估 |
| 4 | 首页健康自检——自动对比首页统计数据与项目实际数据 | P1 | 规划中 |

### 生成管线架构

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
  'primaryColor': '#1e1f2b',
  'primaryTextColor': '#a9b1d6',
  'primaryBorderColor': '#3d59a1', 'lineColor': '#3d59a1',
  'secondaryColor': '#2b2d3b', 'tertiaryColor': '#21232f'
}}}%%
flowchart LR
    SCAN["资产扫描<br/>skills + agents + rules"]:::src --> EXTRACT["数据提取<br/>计数 + 分类 + 路径"]:::proc
    EXTRACT --> TEMPLATE["模板渲染<br/>Handlebars/Mustache"]:::proc
    TEMPLATE --> INJECT["数据注入<br/>替换 {{TOKEN}}"]:::proc
    INJECT --> WRITE["写入 docs/index.html"]:::out
    WRITE --> VERIFY["一致性校验<br/>实际 vs 声明"]:::gate
    VERIFY -->|通过| DONE["✅ 完成"]:::done
    VERIFY -->|不通过| FAIL["❌ 阻断"]:::block
```

### 资产扫描数据 schema

| 层 | 数据源 | 字段 | 类型 |
|---|--------|------|------|
| L1 依赖 | `package.json` | `deps` · `devDeps` | array |
| L2 技能 | `skills/*/SKILL.md` | `name` · `version` · `desc` | array |
| L3 故事 | `cdn/yry-*/README.md` | `title` · `scenes` | array |
| L4 场景 | `cdn/yry-*/scenes/*/index.md` | `name` · `story` | array |
| L5 Agent+规则 | `skills/*/AGENT.md` · `skills/*/rules/*.md` | `role` · `rule` | array |
| L6 参考 | `CLAUDE.md` · `README.md` | `path` · `summary` | array |

### 生成幂等性保证

| 维度 | 策略 | 实现 |
|------|------|------|
| 数据确定性 | 扫描按字母序排序 | `skills.sort()` |
| 模板确定性 | 使用纯函数渲染 | 无随机 · 无时间戳 |
| 输出确定性 | HTML 缩进一致 | prettier 统一 |
| 版本注入 | 仅 header 显示版本 | 不影响数据 |
| 时间戳 | 仅 footer · 独立字段 | diff 时忽略 |

**幂等性测试**:

```bash
node rui-html.mjs --type 首页 > a.html
node rui-html.mjs --type 首页 > b.html
diff a.html b.html  # 期望：仅时间戳行不同
```

### 一致性校验规则

| 维度 | 规则 | 阈值 |
|------|------|:---:|
| 技能数 | 首页计数 vs `ls skills/*/SKILL.md \| wc -l` | = |
| Agent 数 | 首页计数 vs `find skills -name AGENT.md \| wc -l` | = |
| 规则数 | 首页计数 vs `find skills -path '*/rules/*.md' \| wc -l` | = |
| 故事数 | 首页计数 vs `ls cdn/yry-*/README.md \| wc -l` | = |
| 场景数 | 首页计数 vs `find cdn -name 'index.md' \| wc -l` | = |
| 组件数 | 首页计数 vs `ls cdn/yry-* \| wc -l` | = |

### 评审清单

| # | 检查项 | 状态 |
|---|--------|:--:|
| 1 | rui-html 扩展方案确定 | 📋 |
| 2 | 生成模板与手工版功能等价 | 📋 |
| 3 | 资产数据采集脚本完成 | 📋 |

---

> **回溯链**
>
> - 需求来源：本场景由 [故事任务 §7 跨文档索引](../故事任务.md#s-7-跨文档索引) 分配，覆盖 Story 2 FP10，实现首页自动生成管线集成。
> - 基线内容：[故事任务 Story 2 §2 Requirements](../故事任务.md#s-1-story-2) — FP10。
> - 公式约束：遵循 [F.story.scene](../../../../skills/rui/formulas.md) 公式。
> - 证据级别：生成结果可通过 `diff <(cat docs/index.html) <(/rui-html 首页 --stdout)` 验证一致性（证据等级 A）。

### 变更记录

| 日期 | 版本 | 变更内容 | 触发 | 证据 |
|------|------|---------|------|------|
| 2026-06-13 | 1.0.0 | 初始化 | `/rui init` → 场景生成 | 故事任务 Story 2 FP10 |
