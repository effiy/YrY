> | v1.0.0 | 2026-05-22 | deepseek-v4-pro | node .memory/log-interaction.mjs | 🌿 feat/memory-log-interaction-doc | 📎 [CLAUDE.md](../../../CLAUDE.md) |

> **导航**: [← YrY-技术评审](./YrY-技术评审.md) · [YrY-实施报告 →](./YrY-实施报告.md)

> **来源引用**: `/rui doc --from-code .memory-log-interaction-doc`，基于 `YrY-故事任务.md` §5 AC 和 `YrY-使用场景.md` §2 场景

## §0 基线溯源

| TC# | 覆盖 AC#(01 §5) | 覆盖场景(02 §2) | 覆盖类型 | 状态 |
|-----|-----------------|----------------|:--:|:--:|
| TC-N1 | AC1 追加日志 | 场景 1 追加交互日志 | 正常 | 待生成 |
| TC-N2 | AC4 项目名提取 | 场景 1 空状态 | 正常 | 待生成 |
| TC-N3 | AC3 关键决策 | 场景 2 关键决策 | 正常 | 待生成 |
| TC-N4 | AC2 去重 | 场景 3 自动去重 | 正常 | 待生成 |
| TC-B1 | AC2 无文件时去重 | 场景 3 空状态 | 边界 | 待生成 |
| TC-E1 | AC1 缺少必填参数 | 场景 1 异常分支 | 异常 | 待生成 |
| TC-E2 | AC1 命令行参数缺失 | 场景 1 异常分支 | 异常 | 待生成 |
| TC-R1 | AC1 追加后文件一致性 | 全部场景 | 回归 | 待生成 |

### 主要价值

- 🎯 四类用例（正常/边界/异常/回归）全覆盖，Gate A 交接信号完整
- 🔒 异常路径必测：参数缺失、去重逻辑
- ⚡ 每条用例 Given/When/Then 可独立执行，无外部依赖
- 📊 覆盖矩阵对齐全部 AC# 和场景

---

## §1 测试范围

### 1.1 覆盖矩阵

| FP# | 功能点 | 正常 | 边界 | 异常 | 回归 | 覆盖率 |
|-----|--------|:--:|:--:|:--:|:--:|:--:|
| FP1 | 追加日志 | TC-N1, TC-N3 | — | TC-E1, TC-E2 | TC-R1 | 100% |
| FP2 | 会话头生成 | TC-N1 | TC-N2 | — | TC-R1 | 100% |
| FP3 | 去重检测 | TC-N4 | TC-B1 | — | TC-R1 | 100% |
| FP4 | 项目名提取 | TC-N2 | — | — | TC-R1 | 100% |

### 1.2 Gate 映射

| Gate | 用例范围 | 通过标准 | 交接下游 |
|------|---------|---------|---------|
| Gate A | TC-N1–N4, TC-B1, TC-E1–E2 | 全部 P0 通过 | 实现阶段 |
| Gate B | TC-R1 + 全部 Gate A 用例 | P0 100% 通过 | 交付 |

---

## §2 测试用例

### 2.1 正常用例

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|:--:|
| TC-N1 | 目标故事目录存在，无交互日志文件 | 执行 `node .memory/log-interaction.mjs --story=test --turn=1 --agent=pm --user-input="初始化" --assistant-response="完成 CLAUDE.md 生成"` | ① 文件创建；② 含 `> 交互日志 · 追加写入` meta 行；③ 含 `## 会话 <id> — <date>` 头；④ 含 `### HH:mm:ss \| turn-1 \| pm` 条目；⑤ 条目含 `**👤 用户**:` 和 `**🤖 助手**:` | FP1, FP2 | P0 |
| TC-N2 | 未指定 `--project`，CLAUDE.md 含项目名 | 执行追加命令 | 生成的文件以 CLAUDE.md 项目名为前缀（如 `YrY-交互日志.md`） | FP4 | P1 |
| TC-N3 | 传入 `--decisions="架构选型A;技术决策B"` | 执行追加命令 | 条目末尾含 `**📋 关键决策**:` + `- 架构选型A` + `- 技术决策B` | FP1 | P1 |
| TC-N4 | 已有 turn-1 记录 | 再次执行 turn=1 | ① stdout 输出 "跳过: turn-1 已存在"；② 文件内容不变 | FP3 | P0 |

### 2.2 边界用例

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|:--:|
| TC-B1 | 目标故事无交互日志文件 | 执行去重检查 | `hasTurnEntry()` 返回 false → 正常追加（不报错） | FP3 | P1 |
| TC-B2 | 可选参数全部省略 | 仅传入 `--story` + `--turn` + `--agent` | 条目中 `**👤 用户**:` 后为 "—" | FP1 | P2 |

### 2.3 异常用例

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|:--:|
| TC-E1 | — | 执行 `node .memory/log-interaction.mjs --turn=1 --agent=pm`（缺少 --story） | ① stderr 输出 "缺少 --story=<name>"；② 不写入文件 | FP1 | P0 |
| TC-E2 | — | 执行 `node .memory/log-interaction.mjs --story=test --agent=pm`（缺少 --turn） | ① stderr 输出 "缺少 --turn=<N>" | FP1 | P0 |

### 2.4 回归用例

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|:--:|
| TC-R1 | 上次追加的 turn 条目包含完整信息 | 用相同参数重新追加新 turn | ① Markdown 格式一致（meta → 会话头 → turn 条目结构）；② 分隔符 `---` 正确放置 | FP1–FP4 | P0 |

---

## §3 环境专项

| ID | Given | When | Then | 优先级 |
|----|-------|------|------|:--:|
| TC-X1 | 目标故事目录不存在 | 执行追加命令 | 自动递归创建目录，写入成功 | P1 |
| TC-X2 | 交互日志文件为只读 | 执行追加命令 | 文件系统错误被异常抛出 | P2 |

---

## §4 测试环境

| 维度 | 配置 |
|------|------|
| 运行环境 | Node.js ≥ 18（使用 `node:path`, `node:fs` 原生模块） |
| 部署方式 | 本地文件系统，项目根目录下执行 |
| 测试目标 | `.memory/log-interaction.mjs` CLI 行为 |
| 数据准备 | 每次测试前清理或使用临时故事目录 |

---

## §5 评审清单

| # | 检查项 | 状态 |
|---|--------|:--:|
| 1 | 每 FP# 有多类用例覆盖 | ✅ FP1–FP4 含正常+异常 |
| 2 | Gate A 覆盖全部 P0 用例 | ✅ TC-N1, TC-N4, TC-E1, TC-E2 |
| 3 | 回归用例与影响链一致 | ✅ TC-R1 |
| 4 | 异常用例含恢复行为描述 | ✅ |
| 5 | 环境专项覆盖文件生命周期 | ✅ TC-X1, TC-X2 |
| 6 | 无外部依赖（纯 Node.js 标准库） | ✅ |
| 7 | 基线溯源闭合（AC# 全覆盖） | ✅ |

---

## §6 Gate A 交接

| 信号 | 内容 |
|------|------|
| 通过状态 | ✅ 待执行 |
| P0 用例 ID | TC-N1, TC-N4, TC-E1, TC-E2, TC-R1 |
| 实现约束 | 纯 Node.js 标准库，零外部依赖；coder.md 格式不可破坏 |
| 验证命令 | 逐用例手动执行 + `grep` 验证日志输出 |

---

> | 日期 | 变更 | 触发 | 证据 |
> |------|------|------|------|
> | 2026-05-22 | 初始生成 | `/rui doc --from-code .memory-log-interaction-doc` | `YrY-故事任务.md` §5, `YrY-使用场景.md` §2 |
