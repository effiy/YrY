> | v1.0.0 | 2026-05-26 | deepseek-v4-pro | 🌿 feat/rui-import | 📎 [CLAUDE.md](../../../CLAUDE.md) |

> **导航**: [← 技术评审](./技术评审.md) · [安全审计 →](./安全审计.md)

> **来源引用**: 由 `/rui doc --from-code rui-import` 触发。证据 Level B + 规约路径。

[§0 基线溯源](#sec0-baseline) · [§1 测试策略](#sec1-strategy) · [§2 测试范围](#sec2-scope) · [§3 测试用例](#sec3-cases) · [§4 Gate A 交接信号](#sec4-gate-a)

---

### 主要价值

- 🎯 AC 全覆盖 — 每条 AC 至少 1 个测试用例
- 🔒 Gate A 交接信号完整 — P0 用例 ID + 验证命令明确
- ⚡ 覆盖三种同步模式 — 逐文件/批量/pull
- 📊 降级场景独立测试 — no-token/网络失败/单文件失败

---

<a id="sec0-baseline"></a>

## §0 基线溯源

| 基线来源 | 本文档章节 | 映射关系 |
|---------|-----------|---------|
| 故事任务 §5 AC1 | §3.2 用例 1 | 全量同步验收 |
| 故事任务 §5 AC2 | §3.1 用例 2 | 逐文件导入验收 |
| 故事任务 §5 AC3 | §3.3 用例 3 | no-token 降级验收 |
| 故事任务 §5 AC4 | §3.4 用例 4 | 语义标签验收 |

---

<a id="sec1-strategy"></a>

## §1 测试策略

四类用例：正常路径(同步成功) / 边界条件(空目录/无文件) / 异常分支(网络失败/Token 缺失) / 回归验证(路径映射正确性)

---

<a id="sec2-scope"></a>

## §2 测试范围

| 模块 | 测试重点 | 用例数 | 门禁 |
|------|---------|--------|------|
| import-doc.mjs | 逐文件即时导入+语义标签 | 3 | Gate A |
| sync.mjs | 全量扫描+上传+汇总 | 3 | Gate B |
| pull 模式 | 远端下载覆盖本地 | 2 | Gate A |
| 降级处理 | no-token/网络失败/扫描根不存在 | 3 | Gate B |

---

<a id="sec3-cases"></a>

## §3 测试用例

### §3.1 逐文件导入

| # | 类型 | Given | When | Then | 关联 AC |
|---|------|-------|------|------|---------|
| 1 | 正常 | 文件存在，API_X_TOKEN 有效 | `node skills/rui/import-doc.mjs <file>` | 上传成功，输出 ok | AC2 |
| 2 | 异常 | 文件不存在 | 同上 | 跳过，输出 file not found | AC2 |
| 3 | 降级 | API_X_TOKEN 缺失 | 同上 | 输出 no-token skipped | AC2 |

### §3.2 全量同步

| # | 类型 | Given | When | Then | 关联 AC |
|---|------|-------|------|------|---------|
| 4 | 正常 | 项目有 .md + .claude/ | `sync.mjs` | 全部上传，failed=0 | AC1 |
| 5 | 边界 | 项目无 .md 文件 | `sync.mjs` | 仅上传 .claude/ 内容 | AC1 |
| 6 | 异常 | 扫描根不存在 | `sync.mjs dir=/nonexist` | 跳过整次同步 | AC1 |

### §3.3 降级处理

| # | 类型 | Given | When | Then | 关联 AC |
|---|------|-------|------|------|---------|
| 7 | 降级 | API_X_TOKEN 缺失 | `sync.mjs` | 静默跳过，exit 0 | AC3 |
| 8 | 异常 | 网络超时 | `sync.mjs` | 记录告警，exit 1 | AC3 |

### §3.4 语义标签

| # | 类型 | Given | When | Then | 关联 AC |
|---|------|-------|------|------|---------|
| 9 | 正常 | 文件名为 `使用场景.md` | import-doc.mjs 执行 | 远端 tags 含 stage:doc + type:scenario + baseline:problem | AC4 |
| 10 | 边界 | 文件名为 `CLAUDE.md` | import-doc.mjs 执行 | 远端 tags 仅路径标签，无语义标签 | AC4 |

---

<a id="sec4-gate-a"></a>

## §4 Gate A 交接信号

| P0 用例 ID | 验证命令 | 预期结果 | 阻断条件 |
|-----------|---------|---------|---------|
| 1 | `node skills/rui/import-doc.mjs <test-file>` | ok | failed 且非 no-token |
| 4 | `node skills/rui-import/sync.mjs mode=list` | 输出文件清单 | 扫描失败 |

---

> **变更记录**
> | 日期 | 变更 | 触发 | 证据 |
> |------|------|------|------|
> | 2026-05-26 | 初始生成 | /rui doc --from-code rui-import | 故事任务 §5 AC |
