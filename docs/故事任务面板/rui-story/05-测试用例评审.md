> | v1.0 | 2026-05-17 | deepseek-v4-pro | 🌿 main | 📎 [CLAUDE.md](../../../CLAUDE.md) |

> **导航**: [← 02-用户使用场景](./02-用户使用场景.md)

> **来源引用**: 从源码反推生成，源文件 `skills/rui-story/SKILL.md:16-340`。证据等级 B（可推导，附源码路径）。

### 主要价值

- 🧪 全覆盖测试矩阵 — 正常/边界/异常/回归四类用例覆盖全部 10 个功能点
- 🛡️ 安全操作验证 — 删除确认、格式校验、冲突检测等关键路径有独立用例
- 📋 Gate A 明确交接信号 — 通过状态、P0 用例清单、验证命令一目了然
- 🔗 基线溯源完整 — 每个用例关联至 01 §5 AC# 和 02 §2 场景

---

## §0 基线溯源

| TC# | 覆盖 AC#(01 §5) | 覆盖场景(02 §2) | 覆盖类型 | 状态 |
|-----|-----------------|----------------|---------|------|
| TC-N01–N04 | AC1, AC2 | 场景 1: 查看项目整体进度 | 正常 | 待执行 |
| TC-N05–N06 | AC3 | 场景 2: 浏览所有故事详情 | 正常 | 待执行 |
| TC-N07–N09 | AC4, AC5 | 场景 3: 查看单个故事详情 | 正常 | 待执行 |
| TC-N10–N12 | AC6, AC8 | 场景 4: 创建新故事目录 | 正常 | 待执行 |
| TC-N13–N14 | AC9, AC11 | 场景 5: 删除废弃故事 | 正常 | 待执行 |
| TC-N15 | AC12 | 场景 6: 重命名故事 | 正常 | 待执行 |
| TC-N16–N17 | AC13, AC14 | 场景 7: 同步文档到远端 | 正常 | 待执行 |
| TC-N18 | AC15 | 场景 8: 获取命令帮助 | 正常 | 待执行 |
| TC-B01–B04 | AC2, AC8, AC12, AC14 | 场景 1, 4, 6, 7 | 边界 | 待执行 |
| TC-E01–E10 | AC5, AC7, AC8, AC10 | 场景 3, 4, 5, 6, 7 | 异常 | 待执行 |
| TC-R01–R04 | AC1, AC3, AC6, AC9 | 场景 1, 2, 4 | 回归 | 待执行 |

---

## §1 测试范围

### 1.1 覆盖矩阵

| FP# | 功能点 | 正常 | 边界 | 异常 | 回归 | 覆盖率 |
|-----|--------|:----:|:----:|:----:|:----:|:----:|
| FP1 | 状态概览 | ✓ | ✓ | — | ✓ | 完整 |
| FP2 | 进度全景 | ✓ | — | — | ✓ | 完整 |
| FP3 | 单故事详情 | ✓ | — | ✓ | — | 完整 |
| FP4 | 创建故事目录 | ✓ | ✓ | ✓ | ✓ | 完整 |
| FP5 | 删除故事目录 | ✓ | — | ✓ | ✓ | 完整 |
| FP6 | 重命名故事目录 | ✓ | ✓ | ✓ | — | 完整 |
| FP7 | 文档同步 | ✓ | ✓ | ✓ | — | 完整 |
| FP8 | 状态判定 | ✓ | ✓ | — | ✓ | 完整 |
| FP9 | 项目类型推断 | ✓ | — | — | — | 基本 |
| FP10 | 帮助输出 | ✓ | — | — | — | 基本 |

### 1.2 Gate 映射

| Gate | 用例范围 | 通过标准 | 交接下游 |
|------|---------|---------|---------|
| Gate A | 全部正常 + 边界 + 异常（TC-N*, TC-B*, TC-E*） | P0 全部通过，P1 通过率 ≥ 90% | `/rui code rui-story` |
| Gate B | 全部回归（TC-R*）+ 环境专项（TC-X*） | P0 全部通过，无已知 P0 问题 | 交付三步 |

### 1.3 影响链覆盖

| 影响点 | 来源 | 回归用例 | 覆盖状态 |
|--------|------|---------|---------|
| 状态判定逻辑变更 | 01 §2 FP8 | TC-R01 | 待覆盖 |
| create 目录结构变更 | 01 §2 FP4 | TC-R02 | 待覆盖 |
| delete 确认流程变更 | 01 §2 R3 | TC-R03 | 待覆盖 |
| list 输出格式变更 | 01 §2 FP2 | TC-R04 | 待覆盖 |

---

## §2 测试用例

### 2.1 正常用例 (TC-N*)

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|--------|
| TC-N01 | 故事面板下存在 3 个故事：`story-a`（01 存在→docs_in_progress）、`story-b`（01+02+05→docs_done）、`story-c`（空目录→not_started） | 执行 `/rui-story` | 输出状态统计：not_started=1, docs_in_progress=1, docs_done=1；其余状态=0；合计 3 个故事；最近活动显示最近修改的故事 | FP1, FP8 | P0 |
| TC-N02 | 故事面板下存在 1 个被阻断的故事（`.memory/rui-state.json` 含 `blocked=true`） | 执行 `/rui-story` | blocked 计数 = 1，其他状态 = 0 | FP1, FP8 | P0 |
| TC-N03 | 故事面板目录为空 | 执行 `/rui-story` | 所有状态计数 = 0，合计 0 个故事，最近活动显示"无" | FP1 | P0 |
| TC-N04 | `docs/故事任务面板/` 目录不存在 | 执行 `/rui-story` | 所有状态计数 = 0，合计 0 个故事，不报错 | FP1 | P0 |
| TC-N05 | 故事面板下存在 2 个故事，分别有不同文件数和修改时间 | 执行 `/rui-story list` | 输出表格含 Story/Status/Files/Last Modified/Type/Branch 六列，按最后修改降序排列 | FP2, FP9 | P0 |
| TC-N06 | 某故事有关联的 git 分支 `feat/story-a` | 执行 `/rui-story list` | 该故事的 Branch 列显示 `feat/story-a` | FP2 | P0 |
| TC-N07 | 故事 `my-story` 目录存在，含 01+02+05 三份文档 | 执行 `/rui-story show my-story` | 输出详述卡：状态=docs_done、类型=meta、文件清单含 3 个文件及大小/时间、git 分支=无 | FP3 | P1 |
| TC-N08 | 故事 `my-story` 元数据含阻断信息 | 执行 `/rui-story show my-story` | 详述卡中显示状态=blocked、阻断原因可见 | FP3 | P1 |
| TC-N09 | 故事 `my-story` 有关联分支 `feat/my-story` | 执行 `/rui-story show my-story` | 详述卡中 git 分支显示 `feat/my-story` | FP3 | P1 |
| TC-N10 | 名称 `user-login` 格式合法，目标目录不存在 | 执行 `/rui-story create user-login` | 创建 `docs/故事任务面板/user-login/` 和 `.memory/story-type.json`，无其他文件；显示创建成功确认 | FP4 | P0 |
| TC-N11 | 名称 `user-login` 格式合法，目标目录不存在，指定 `--type frontend` | 执行 `/rui-story create user-login --type frontend` | `.memory/story-type.json` 中 type 为 `frontend` | FP4, FP9 | P0 |
| TC-N12 | 名称 `user-login` 格式合法，目标目录不存在，未指定 `--type` | 执行 `/rui-story create user-login` | `.memory/story-type.json` 中 type 为 `meta`（默认值） | FP4, FP9 | P1 |
| TC-N13 | 故事 `old-story` 目录存在，无关联 git 分支 | 执行 `/rui-story delete old-story`，确认输入 `yes` | 无分支警告，删除目录成功，显示删除确认 | FP5 | P0 |
| TC-N14 | 故事 `old-story` 目录存在，有关联分支 `feat/old-story` | 执行 `/rui-story delete old-story`，确认输入 `yes` | 先警告"分支 feat/old-story 将被保留"，然后删除目录 | FP5, R4 | P0 |
| TC-N15 | 名称 `old-name` 和 `new-name` 格式均合法，old 存在，new 不存在，无关联分支 | 执行 `/rui-story rename old-name new-name` | 目录从 `old-name` 重命名为 `new-name`，内容完整保留 | FP6 | P0 |
| TC-N16 | 故事 `my-story` 目录存在 | 执行 `/rui-story sync my-story` | 委托同步程序同步 `docs/故事任务面板/my-story/`，显示同步结果 | FP7 | P1 |
| TC-N17 | 不指定故事名称 | 执行 `/rui-story sync` | 委托同步程序同步全部 `docs/故事任务面板/`，显示同步结果 | FP7 | P1 |
| TC-N18 | 用户需要查看帮助 | 执行 `/rui-story --help`（或 `-h` / `help`） | 输出完整帮助文本，含用法、子命令列表、场景示例、操作边界、核心规则 | FP10 | P1 |

### 2.2 边界用例 (TC-B*)

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|--------|
| TC-B01 | 故事面板下有大量故事（如 100 个） | 执行 `/rui-story` | 正确统计并显示，输出在合理时间内完成（< 5 秒） | FP1 | P1 |
| TC-B02 | 名称恰好为最小长度（如 `a`） | 执行 `/rui-story create a` | 创建成功 | FP4 | P1 |
| TC-B03 | 名称包含多个连字符（如 `my-very-long-story-name`） | 执行 `/rui-story create my-very-long-story-name` | 创建成功 | FP4 | P1 |
| TC-B04 | 同步时指定了不存在的故事名称 | 执行 `/rui-story sync not-exist` | 同步程序报错透传给用户 | FP7 | P1 |

### 2.3 异常用例 (TC-E*)

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|--------|
| TC-E01 | — | 执行 `/rui-story show not-exist`（目录不存在） | 报错提示"故事不存在" | FP3 | P1 |
| TC-E02 | — | 执行 `/rui-story show UserLogin`（名称含大写） | 报错提示名称需为 kebab-case 格式 | FP3 | P1 |
| TC-E03 | — | 执行 `/rui-story create UserLogin`（名称含大写） | 报错提示名称需为 kebab-case 格式 | FP4 | P1 |
| TC-E04 | — | 执行 `/rui-story create user-login`（名称含特殊字符如 `_`） | 报错提示名称格式不合法 | FP4 | P1 |
| TC-E05 | 故事 `existing` 目录已存在 | 执行 `/rui-story create existing` | 拒绝覆盖，提示目录已存在，建议使用更新命令 | FP4 | P0 |
| TC-E06 | — | 执行 `/rui-story delete not-exist`（目录不存在） | 报错提示"故事不存在" | FP5 | P0 |
| TC-E07 | 故事 `old-story` 目录存在 | 执行 `/rui-story delete old-story`，输入 `no`（或直接回车） | 取消操作，目录保留，提示操作已取消 | FP5 | P0 |
| TC-E08 | — | 执行 `/rui-story rename not-exist new-name`（old 不存在） | 报错提示旧名称故事不存在 | FP6 | P0 |
| TC-E09 | 故事 `old-name` 存在，`new-name` 目录也存在 | 执行 `/rui-story rename old-name new-name` | 拒绝覆盖，提示新名称已被占用 | FP6 | P0 |
| TC-E10 | — | 执行 `/rui-story rename OldName new-name`（old 含大写） | 报错提示名称格式不合法 | FP6 | P0 |

### 2.4 回归用例 (TC-R*)

| ID | Given | When | Then | 关联 FP | 优先级 |
|----|-------|------|------|---------|--------|
| TC-R01 | 创建故事后，再添加文档基线（01→02→05） | 每次添加文档后执行 `/rui-story` | 状态从 not_started → docs_in_progress → docs_done 正确流转 | FP1, FP8 | P0 |
| TC-R02 | 通过 create 创建目录 | 检查产物 | 目录存在、`.memory/story-type.json` 存在、无任何 `.md` 文档文件 | FP4 | P0 |
| TC-R03 | 删除操作的不同确认输入 | 分别尝试 `yes`、`YES`、`y`、`no`、空输入 | 仅 `yes`（精确匹配）执行删除 | FP5, R3 | P0 |
| TC-R04 | 创建新故事后 | 执行 `/rui-story list` | 新故事出现在列表最顶部（按修改时间降序），各列信息正确 | FP2 | P1 |

---

## §3 环境专项

| TC-X# | ID | Given | When | Then | 优先级 |
|-------|-----|------|------|------|--------|
| TC-X01 | 生命周期 | 面板目录存在但权限为只读 | 执行 create/delete/rename 写操作 | 报错提示权限不足 | P1 |
| TC-X02 | 通信通道 | 网络不可用 | 执行 sync | 同步程序报网络错误，消息透传给用户 | P1 |
| TC-X03 | 存储 | 面板目录在符号链接路径下 | 执行 create/delete | 操作正确作用于实际目录，符号链接关系保持 | P2 |
| TC-X04 | 存储 | 故事目录包含非标准文件（如 `.DS_Store`） | 执行 `/rui-story` 和 `/rui-story show` | 状态判定不受干扰，文件清单正常列出 | P2 |

---

## §4 测试环境

| 维度 | 配置 |
|------|------|
| 运行环境 | Node.js（help.mjs 脚本执行）+ Bash（目录操作） |
| 部署方式 | 本地开发环境，YrY 插件已安装 |
| 测试目标 | `docs/故事任务面板/` 目录及子目录的 CRUD 操作、状态判定逻辑 |
| 数据准备 | 预置多种状态的故事目录作为测试夹具 |

---

## §5 评审清单

| # | 检查项 | 状态 |
|---|--------|:----:|
| 1 | 每功能点多类覆盖（正常+边界+异常） | ✅ |
| 2 | Gate A 覆盖 — P0 用例全部标记 | ✅ TC-N01/05/10/13 等 |
| 3 | 回归用例与影响链一致 | ✅ 4 条影响点均有回归 |
| 4 | 异常用例含恢复行为描述 | ✅ 每题 Then 明确报错提示或取消行为 |
| 5 | 环境专项覆盖 — 生命周期/通信/存储 | ✅ TC-X01–X04 |
| 6 | 无外部依赖占比合理 | ✅ 全部用例可本地验证 |
| 7 | 影响链每点有回归 | ✅ TC-R01–R04 |
| 8 | 基线溯源闭合 — 全部 AC# 和场景有对应用例 | ✅ §0 表完整 |

---

## §6 Gate A 交接

| 信号 | 内容 |
|------|------|
| 通过状态 | 待验证 — 需执行全部 TC-N*、TC-B*、TC-E* 用例 |
| P0 用例 | TC-N01, TC-N02, TC-N03, TC-N04, TC-N05, TC-N06, TC-N10, TC-N11, TC-N13, TC-N14, TC-N15, TC-E05, TC-E06, TC-E07, TC-E08, TC-E09, TC-E10, TC-R01, TC-R02, TC-R03 |
| 实现约束 | 仅管理目录结构，禁止创建文档内容；不可绕过用户确认；命名强制 kebab-case |
| 验证命令 | `grep -c "TC-" 05-测试用例评审.md` 统计用例总数；逐用例手工或脚本执行验证 |

---

## 变更记录

| 日期 | 变更 | 触发 | 证据 |
|------|------|------|------|
| 2026-05-17 | 初始生成 | `/rui doc --from-code rui-story` | 源码 `skills/rui-story/SKILL.md`、公式 `skills/rui/formulas.md` |
