/**
 * ruiBadge · config data source
 * ----------------------------------------------------------------------
 * Static config for the badge component.
 * Exposed via window.rui_BADGE_CONFIG and read by index.js.
 */

window.rui_BADGE_CONFIG = {
    templateId:    'rui-badge-tpl',
    loadTimeoutMs: 5000,
    defaults: {
        modifier: 'info',
        size:     'md'
    }
};
