/**
 * YrY · H5 UI shared component styles
 *
 * Aggregated CSS module for reusable UI primitives:
 *   • .iconbtn / .iconbtn--sort / .iconbtn--new    Icon action buttons
 *   • .searchCard / .listCard                       Card wrappers
 *   • .pillBtn                                      Pill action button
 *   • .chips / .chip / .chip__x / .chip__count      Chip/tag list
 *   • .tagRow / .tagPill / .tagPill__count          Horizontal tag filter row
 *   • .seg / .seg__btn                               Segmented control
 *   • .ghostBtn                                      Ghost button
 *   • .badge / .badge.is-green / .badge.is-red       Status badges
 *   • .miniBtn                                       Compact button
 *   • .empty / .empty__icon / .empty__title          Empty state
 *   • .manageBar / .manageBar__left / .manageBar__right  Bottom bar
 *   • .drawer / .drawerMask / .drawer__header         Slide-up drawer
 *   • .sheet / .sheetMask / .sheet__header            Bottom sheet
 *   • .msgs / .msg / .msg__role / .msg__content       Message list
 *   • .faqHint / .faqList / .faqItem / .faqEmpty      FAQ
 *   • .selBox / .dot / .pinIcon                       Misc utilities
 *
 * Includes merged styles from styles/components/pet.css (Chrome pet).
 * All styles live in index.css. No JavaScript runtime.
 */
export const styles = '/cdn/components/business/h5/UI/index.css';
