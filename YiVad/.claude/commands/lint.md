---
description: Run all linters — ESLint + Prettier + Stylelint
---

# /lint

Run all code quality checks and auto-fix where possible.

```bash
cd YiVad && npm run lint:eslint && npm run lint:prettier && npm run lint:stylelint
```

Run type-check separately: `npm run type:check`
