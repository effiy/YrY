---
description: Start Vite dev server with HMR
---

# /dev

Start the YiVad Vite dev server with hot module replacement.

```bash
cd YiVad && npm run dev
```

The dev server starts on the port configured in `vite.config.ts`.
